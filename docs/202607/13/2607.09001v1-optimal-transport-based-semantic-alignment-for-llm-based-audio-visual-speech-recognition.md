---
title: Optimal Transport-based Semantic Alignment for LLM-based Audio-Visual Speech Recognition
title_zh: 基于最优传输的语义对齐用于基于大语言模型的音视频语音识别
authors: "Xugang Lu, Peng Shen, Yu Tsao, Hisashi Kawai"
date: 2026-07-10
pdf: "https://arxiv.org/pdf/2607.09001v1"
tags: ["query:post-multi"]
score: 8.0
evidence: 面向音频视觉语音识别的多模态语义对齐
tldr: 基于大语言模型的视听语音识别在嘈杂环境下表现鲁棒，但现有方法未显式解决视听与文本模态的表示差异。本文提出最优传输语义对齐框架，通过计算模态特征与语言嵌入的概率耦合矩阵作为软伪标签，监督对比学习实现跨模态对齐。在LRS3-TED数据集上，该方法在干净和噪声条件下均达到最优性能，显著超越强基线。该工作通过将多模态特征锚定到语言空间，有效提升了融合与解码效果。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.09001v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1554, \"height\": 929, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09001v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 621, \"height\": 363, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.09001v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 771, \"height\": 247, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09001v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 771, \"height\": 248, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09001v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 785, \"height\": 137, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09001v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 724, \"height\": 271, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09001v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 859, \"height\": 314, \"label\": \"Table\"}]"
motivation: 现有方法未显式对齐视听与文本表示，限制跨模态融合效果。
method: 利用最优传输估计模态特征与语言嵌入的耦合矩阵，作为软伪标签监督对比学习。
result: 在LRS3-TED上，干净及噪声条件均取得最佳性能，超越强基线。
conclusion: OT语义对齐框架有效桥接模态差异，提升LLM-AVSR鲁棒性。
---

## 摘要
基于大语言模型的音视频语音识别（LLM-AVSR）最近通过利用互补的音频和视觉信息，在不利的声学环境中展现出强大的鲁棒性。现有方法通常采用独立预训练的声学编码器和视觉编码器，将它们的输出投影并融合为软提示，以条件化大语言模型进行语音识别。然而，大多数方法在执行多模态融合时未明确解决音频、视觉和文本模态之间的表示差异，可能限制了跨模态集成的有效性。本文提出了一种基于最优传输（OT）的语义对齐框架用于LLM-AVSR。该方法在多模态融合之前，通过将声学和视觉表示相对于大语言模型的语言嵌入空间进行对齐，显式地弥合了模态差距。具体地，使用OT估计概率耦合矩阵，该矩阵表征模态特定特征与语言嵌入之间的结构化对应关系。进一步将得到的OT耦合作为软伪标签来监督对比学习，鼓励提取语义连贯且跨模态一致的音视频表示。通过将多模态特征锚定到大语言模型的语言空间，所提框架促进了更有效的多模态融合和解码。我们使用基于Whisper的声学编码器、基于AV-HuBERT的视觉编码器和LLaMA3.2-3B解码器实现了所提框架。在LRS3-TED基准上进行的实验表明，该方法在强基线上取得了持续改进，并在广泛的信噪比下的清洁和噪声评估条件下均达到了最先进的性能。

## Abstract
Large language model (LLM)-based audio-visual speech recognition (LLM-AVSR) has recently demonstrated strong robustness in adverse acoustic environments by leveraging complementary audio and visual information. Existing approaches typically employ independently pretrained acoustic and visual encoders, whose outputs are projected and fused as soft prompts to condition an LLM for speech recognition. However, most methods perform multimodal fusion without explicitly addressing the representational discrepancy between audio, visual and text modalities, potentially limiting the effectiveness of cross-modal integration. In this paper, we propose an optimal transport (OT)-based semantic alignment framework for LLM-AVSR. The proposed method explicitly bridges the modality gap by aligning the acoustic and visual representations with reference to the linguistic embedding space of the LLM before multimodal fusion. Specifically, OT is used to estimate probabilistic coupling matrices that characterize structured correspondences between modality-specific features and linguistic embeddings. The resulting OT couplings are further utilized as soft pseudo-labels to supervise contrastive learning, encouraging the extraction of semantically coherent and cross-modal consistent audio-visual representations. By anchoring multimodal features to the linguistic space of the LLM, the proposed framework facilitates more effective multimodal fusion and decoding. We implement the proposed framework using a Whisper-based acoustic encoder, an AV-HuBERT-based visual encoder, and a LLaMA3.2-3B decoder. Experiments conducted on the LRS3-TED benchmark demonstrate consistent improvements over strong baselines and achieve state-of-the-art performance under both clean and noisy evaluation conditions across a wide range of signal-to-noise ratios (SNRs).

---

## 论文详细总结（自动生成）

# 基于最优传输的语义对齐用于基于大语言模型的音视频语音识别——论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有基于大语言模型（LLM）的音视频语音识别系统（LLM-AVSR）在处理多模态融合时，通常直接投影并拼接声学和视觉特征，但未显式解决**声学、视觉与文本三种模态之间的表示差异**（representational discrepancy）。由于声学编码器（如Whisper）和视觉编码器（如AV-HuBERT）是独立用不同目标和数据预训练的，它们的特征空间存在结构性不匹配，简单融合难以充分利用互补信息。此外，**时间同步未必对应语义同步**，直接对齐时间帧可能无法有效消解歧义。
- **整体含义**：为了提升LLM-AVSR在噪声环境下的鲁棒性和识别精度，需要**在融合之前，先将多模态特征显式地锚定到LLM的语言嵌入空间**，实现语义层面的对齐。本文提出基于最优传输（Optimal Transport, OT）的对齐框架，将声、视特征与文本嵌入作为概率分布，计算结构化对应关系，进而通过对比学习强化跨模态一致性，从而改善融合和解码效果。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将不同模态的特征序列视为经验概率分布，利用最优传输（OT）计算声学特征→文本嵌入、视觉特征→文本嵌入之间的软耦合矩阵（probabilistic coupling matrix），以此作为软伪标签监督对比学习，迫使模型学习与语言空间语义对齐的跨模态一致表示。
- **技术框架（图1）**：
    1. **特征编码**：
        - 声学：Whisper 编码器 → 线性投影 → \( \mathbf{z}_a \in \mathbb{R}^{L \times d} \)
        - 视觉：AV-HuBERT 编码器 → 线性投影 → \( \mathbf{z}_v \in \mathbb{R}^{L \times d} \)
        - 文本：目标文本经 LLM 的 token 嵌入层 → 线性全连接 → \( \mathbf{z}_t \in \mathbb{R}^{L_t \times d} \)（作为参考分布）
    2. **OT 对齐模块**：
        - 将 \( \mathbf{z}_a \) 与 \( \mathbf{z}_t \) 视为分布 \( \mu_a, \mu_t \)，求解熵正则化 OT：
          \[
          \max_{Q_{a\to t} \in \mathcal{Q}} \langle Q_{a\to t}, S_{a\to t} \rangle + \lambda H(Q_{a\to t})
          \]
          其中相似度矩阵 \( S_{a\to t} \) 由余弦相似度计算，\( Q \) 为耦合矩阵。可通过 Sinkhorn 算法迭代求解。
        - 同理对视觉-文本计算 \( Q_{v\to t} \)。
    3. **虚拟桶（Virtual Bucket）机制**：引入额外占位符，允许非信息帧（如静音、背景噪声）不被强制对齐到任何语言 token，避免噪声帧干扰语义匹配（图2）。
    4. **对齐损失**：将OT耦合矩阵 \( Q \) 作为软标签，对对比学习概率（softmax 化的相似度）施加交叉熵损失：
       \[
       L_{\text{align}} = -\sum_{i,j} \big( Q^{a\to t}_{i,j} \log P^{a\to t}_{i,j} + Q^{v\to t}_{i,j} \log P^{v\to t}_{i,j} \big)
       \]
    5. **多模态融合**：对比了四种融合方式：通道拼接（C Concat）、加法（Add）、交叉注意力（Cross att）、Q-Former。实验后选择通道拼接作为最终方案。
    6. **总体损失**：\( L_{\text{total}} = L_{\text{AR}} + \alpha L_{\text{align}} \)，其中 \( L_{\text{AR}} \) 为 LLM 自回归交叉熵损失。

- **算法流程（训练阶段）**：
    - 输入：指令文本、音频、视频、目标文本
    - 分别提取 \( \mathbf{z}_a, \mathbf{z}_v, \mathbf{z}_t \)
    - 计算 OT 耦合矩阵 \( Q_{a\to t}, Q_{v\to t} \)（含虚拟桶）
    - 计算对比式对齐损失 \( L_{\text{align}} \)
    - 融合特征得到 \( \mathbf{h}_{av} \)，与指令嵌入一起送入 LLM（LoRA 微调）生成转录
    - 更新 \( L_{\text{total}} \)

## 3. 实验设计

- **数据集**：LRS3-TED 基准数据集（433小时训练集、1小时验证集、1小时测试集）。
- **评估场景**：
    - 清洁测试（clean）
    - 噪声测试：在测试音频中添加 babble 噪声，信噪比（SNR）分别为 -5 dB、0 dB、5 dB。
- **对比方法**：
    - 基线自我对比：不同融合方式（C Concat、Add、Cross att、Q-Former、T Concat）
    - 无对齐 vs 有对齐（OT with/without virtual bucket）
    - 不同对齐权重 \( \alpha \)
    - 外部先进方法：Whisper-Flamingo、LLaMA-AVSR、MMS-LLaMA
    - 本文最佳设置（三种超参数组合）
- **指标**：词错误率（WER%）。

## 4. 资源与算力

- **文中未明确给出** GPU 型号、数量或训练时长。
- 仅提及优化细节：Adam 优化器，初始学习率 1e-4，5k warm-up steps，总训练步数 30k，LoRA rank=16，缩放因子=32。
- 因此无法量化具体算力开销。需指出该信息缺失。

## 5. 实验数量与充分性

- **实验组数**：
    - 表 I/II：清洁/噪声训练条件下 5 种融合方式对比（共 2×5×4=40 个WER值）
    - 表 III：OT 有无虚拟桶对比（2×4=8个WER值）
    - 表 IV：不同对齐权重 \( \alpha \)（6个值×4个SNR=24个WER值）
    - 表 V：与 3 种外部方法 + 自身无对齐 + 3 种设置对比（7方法×4个SNR=28个WER值）
    - 此外还做了超参数（熵正则化 \(\lambda\)、温度 \(\tau\)、虚拟桶边际 \(\tilde{s}\)）的初步探究，但仅定性描述无具体表格。
- **充分性与公平性**：
    - 实验覆盖了清洁与多种噪声 SNR，消融全面（融合方式、对齐机制、权重、虚拟桶）。
    - 对比的外部方法均引用了原始论文，与本文基础架构相似，比较公平。
    - 不足：超参数组合未做全面网格搜索（仅凭经验选取），且缺少跨数据集或跨语言验证。

## 6. 论文的主要结论与发现

- **OT 语义对齐显著提升性能**：在所有 SNR 和清洁条件下，带 OT 对齐的模型均优于无对齐基线（如无对齐基线在 SNR-5 时 WER 8.34%，OT+虚拟桶设置可达 7.16%）。
- **虚拟桶有助于排除非信息帧**：带虚拟桶的对齐略优于不带虚拟桶（SNR-5: 7.98% vs 8.06%），证明其过滤噪声帧的有效性。
- **通道拼接融合最优**：在噪声训练条件下，通道拼接（C Concat）表现出最好的鲁棒性（SNR-5: 8.34%，其他融合均高于此）。
- **对齐权重** \(\alpha=0.1\sim0.5\) 时效果较好，过大或过小会降低性能。
- **与现有 SOTA 比较**：本文最佳设置（setting 1: \(\alpha=0.1,\tilde{s}=0.5,\lambda=0.2\)）在 SNR-5 时 WER 7.61%，低于 MMS-LLaMA 的 7.44%？实际表 V 中 setting 3 为 7.16%，成为新 SOTA。
- 综上，**OT 语义对齐能够有效弥合模态差异，提升 LLM-AVSR 的鲁棒性与准确性**。

## 7. 优点

- **方法创新性**：首次在 LLM-AVSR 中使用最优传输显式对齐声学/视觉与语言空间，而非简单拼接。
- **机制灵活**：虚拟桶的引入有效处理非信息帧，与熵正则化 OT 结合使对齐更鲁棒。
- **实验全面**：覆盖多种融合策略、对齐组件、噪声条件，消融实验完整。
- **SOTA 成果**：在 LRS3-TED 上达到最佳 WER，且实现简洁（无需修改预训练编码器/LLM 结构，仅插入对齐模块）。
- **开源友好**：使用公开模型（Whisper、AV-HuBERT、LLaMA3.2-3B），易于复现。

## 8. 不足与局限

- **超参数敏感且未充分优化**：OT 相关超参数（\(\lambda, \tilde{s}, \tau\)）与对齐权重 \(\alpha\) 之间存在交互作用，文中仅凭经验选取，未进行系统联合优化。这可能导致性能尚未达到理论最佳。
- **单数据集评估**：仅在 LRS3-TED 上实验，缺乏跨库、跨语言或更复杂场景（如多说话人、极低光照）的验证。
- **缺乏算力报告**：未提供训练耗时、GPU 型号/数量，不利于社区复现比较。
- **只考虑固定编码器/LLM**：声学/视觉编码器被冻结，LLM 仅用 LoRA 微调，未探索全参数微调或更大规模模型的效果。
- **未讨论领域外噪声**：仅使用 babble 噪声，未检验其他噪声类型（如音乐、交通噪声），泛化性需进一步验证。

（完）
