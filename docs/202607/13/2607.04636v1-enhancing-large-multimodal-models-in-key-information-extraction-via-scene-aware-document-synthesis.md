---
title: Enhancing Large Multimodal Models in Key Information Extraction via Scene-Aware Document Synthesis
title_zh: 通过场景感知文档合成增强关键信息提取中的大型多模态模型
authors: "Zhipeng Xu, Zulong Chen, Qing Liu, Junhao Ji, Jinxin Hu, Yipeng Yu, Jianqiang Wan, Jun Tang, Zhao Li"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.04636v1"
tags: ["query:post-multi"]
score: 8.0
evidence: 通过文档合成增强多模态模型训练
tldr: 视觉文档关键信息提取（KIE）面临性能与部署成本的权衡，服务器端大语言模型（LMM）昂贵但效果好，本地模型则缺乏训练数据。本文提出SAYRE，一种场景感知文档合成框架，从少量示例文档自动合成训练三元组，并通过错误驱动生成困难样本。实验表明，SAYRE显著提升Qwen3-VL模型在约束和开放KIE任务上的表现，在设备端LMM中达到最优。合成数据越多性能越强，尤其对小型模型，错误分析证实合成训练增强了模式感知提取能力。该数据驱动方法有效提升多模态KIE在实际场景中的泛化性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04636v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 807, \"height\": 901, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04636v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 774, \"height\": 605, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04636v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1538, \"height\": 917, \"label\": \"Table\"}]"
motivation: 紧凑型多模态模型缺乏足够KIE训练数据，手工模板设计成本高且难以扩展。
method: SAYRE从少量示例文档学习内容与布局模式，自动合成文档-模式-标注三元组，并用错误驱动生成增强困难样本。
result: 在约束与开放KIE任务中，SAYRE持续提升Qwen3-VL性能，设备端LMM达最优，数据扩展进一步改善小型模型。
conclusion: 场景感知文档合成无需手工设计即可为多模态KIE提供高质量训练数据，是一种有效数据驱动方法。
---

## 摘要
关键信息提取（KIE）将视觉丰富的文档转换为结构化数据，但实际部署仍然具有挑战性：强大的性能通常依赖于昂贵的服务器端大型多模态模型（LMM），而紧凑的本地可部署模型缺乏足够的KIE监督。我们提出了SAYRE，一种场景感知的文档合成框架，用于生成可扩展的KIE训练数据，无需手工设计模板。给定少量示例文档，SAYRE捕获类别特定的内容模式和布局约定，以合成文档-模式-注释三元组。它进一步引入了错误驱动生成，将现实世界的失败案例扩展为困难训练示例，同时保留其结构模式。在受限类别和开放类别KIE上的实验表明，SAYRE持续改进了Qwen3-VL骨干网络，并在设备端LMM中取得了最强的整体性能。数据扩展实验显示，随着引入更多合成数据，整体呈上升趋势，尤其是对于较小模型和开放类别提取。错误分析进一步表明，合成训练通过改善对密集表格、商业标识符和合同条款的模式感知提取，减少了字段级错误。这些结果确立了场景感知合成作为一种有效的数据中心方法，用于改进实际多模态KIE。

## Abstract
Key Information Extraction (KIE) converts visually rich documents into structured data, but practical deployment remains challenging: strong performance often relies on costly on-server Large Multimodal Models (LMMs), while compact locally deployable models lack sufficient KIE supervision. We present SAYRE, a scene-aware document synthesis framework for generating scalable KIE training data without hand-crafted template design. Given a few exemplar documents, SAYRE captures category-specific content patterns and layout conventions to synthesize document-schema-annotation triples. It further introduces error-driven generation, which expands real-world failure cases into hard training examples while preserving their structural patterns. Experiments on constrained- and open-category KIE show that SAYRE consistently improves Qwen3-VL backbones and achieves the strongest overall performance among on-device LMMs. Data scaling experiments show an overall upward trend as more synthesized data is introduced, especially for smaller models and open-category extraction. Error analysis further shows that synthesized training reduces field-level errors by improving schema-aware extraction over dense tables, business identifiers, and contract clauses. These results establish scene-aware synthesis as an effective data-centric approach for improving practical multimodal KIE.

---

## 论文详细总结（自动生成）

# 论文《Enhancing Large Multimodal Models in Key Information Extraction via Scene-Aware Document Synthesis》中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **核心问题**：关键信息提取（KIE）任务面临性能与部署成本的权衡。服务器端大型多模态模型（LMM）性能强但成本高、延迟大，不适合企业本地部署；而本地可部署的紧凑型 LMM 缺乏足够的 KIE 训练数据，提取能力不足。
- **研究动机**：现有文档合成方法依赖手工模板或简单内容替换，难以扩展至多种文档类别，且未能覆盖真实场景中的失败模式，导致合成数据多样性不足、对困难案例覆盖有限。
- **整体含义**：本文提出一种**无手工模板**的场景感知文档合成框架 SAYRE，通过从少量示例文档自动学习内容模式与布局惯例，生成文档-模式-标注三元组，并利用错误驱动生成困难样本，从而为紧凑型 LMM 提供高质量、可扩展的训练数据，缩小与服务器端模型的性能差距。

## 2. 方法论
### 核心思想
- SAYRE 由一个**通用数据生成**管道和一个**错误驱动生成**管道组成，无需预定义模板，仅需目标类别的少量示例文档。
- 通过多智能体系统（感知、生成代理）捕获文档类别的语义与视觉特征，并利用 persona card 增加内容多样性。

### 关键技术细节
1. **通用数据生成 (General Data Generation)**：
   - 输入：类别标识符 \(I_c\)，persona card \(P\)，以及 \(n\) 个示例文档 \(\{x_i\}\)。
   - 流程：
     - 主题生成：\(t = V_t(I_c, P)\)。
     - 内容感知与布局感知：使用 VLM 代理 \(V_C\) 提取内容描述 \(C\)（包括常见字段、语义、依赖关系），使用布局代理 \(V_L\) 提取布局描述 \(L\)（页面大小、层次结构、空间分布）。
     - 结构化输出与模式生成：内容生成代理 \(V_J\) 基于 \(C, t, P\) 生成结构化输出 \(y\)，模式 \(S\) 由 \(y\) 去除字段值后得到。
     - 文档合成：文档生成代理 \(V_D\) 根据 \(y\) 和 \(L\) 生成 HTML 代码 \(H_x\)，再渲染为文档图像 \(x\)。
     - （可选）按预定义模式 \(S'\) 进行字段筛选，适配具体应用场景。

2. **错误驱动数据生成 (Error-driven Data Generation)**：
   - 收集真实失败案例（来自模型测试和生产系统），人工标注确保高质量。
   - 使用代理 \(V_E\) 将每个失败案例转换为 HTML 模板。
   - 提取文本块并建立与标签的映射，然后通过 LLM 重写文本进行脱敏，更新标签。
   - 对无法可靠映射的字段，使用多模型投票策略重新标注。

3. **视觉退化模拟**：对合成图像添加真实光学噪声（使用 Blender 工具），模拟实际文档采集条件。

### 公式/算法流程（文字说明）
- 通用生成过程：\( (x, S, y) \sim G(\{x_i\}_{i=1}^n, I_c, P) \)
- 错误驱动过程：将每个失败案例 \(x'\) 转换成 HTML 模板，提取并重写文本内容，再渲染为新文档 \(\tilde{x}'\)，同时更新标签 \(\tilde{y}'\) 和模式 \(S'\)。

## 3. 实验设计
- **数据集/场景**：使用 **UniKIE** 基准，覆盖约束类别（Constrained-Category）和开放类别（Open-Category），包括 Business Transaction、Public Service、Registration Record、Receipt、Form、Invoice、Contract 等。
- **评估指标**：字段级 F1 分数（严格精确匹配，经归一化处理）。
- **对比方法**：
  - **设备端 LMM 基线**：MiniCPM-V4.5-8B、InternVL3.5-8B、Qwen3-VL-2B/4B、Ministral-3-8B、MiMo-VL-7B-RL、GLM-4.1V-9B。
  - **服务器端 LMM 上限**：Claude-Sonnet-4.5、GPT-4o、GPT-5、Qwen-VL-Max、Qwen3-VL-Plus、Gemini-3-Pro。
- **SAYRE 模型**：基于 Qwen3-VL-2B 和 Qwen3-VL-4B 在合成数据上微调 15K 步。

## 4. 资源与算力
- 文中明确说明：
  - 使用 **4 张 NVIDIA A800 GPU**（显存未提，推断为 80GB 版本）。
  - 分布式训练采用 **DeepSpeed ZeRO Stage 2**，全参数微调。
  - 每设备 batch size = 4，梯度累积 4 步 → 有效全局 batch size = 64。
  - 训练步数：**15K 步**。
  - 精度：bfloat16。
  - 最大序列长度 8192，最大图像分辨率 1,605,632 像素。

## 5. 实验数量与充分性
- **总体实验数量**：主要包括：
  - 整体性能对比（表1）：对比 SAYRE 模型与 6 个服务器端模型 + 7 个设备端模型，报告 7 个文档类别的 F1。
  - 数据扩展实验（图1）：以不同训练步数（0~14K）绘制 F1 和增益曲线，分析约束/开放/KIE 平均 F1 及增益。
  - 错误分析（图2）：对比 Qwen3-VL-4B 与 SAYRE-4B 在 6 类字段上的错误计数和相对误差减少比例。
- **充分性与公平性**：
  - 实验覆盖了从 2B 到 4B 的容量、从固定模式到开放场景、从简单到复杂文档类别，维度比较全面。
  - 与多种同类模型在同一基准上公平对比，指标采用严格精确匹配，避免作弊。
  - 扩展实验中，两个模型规模均显示一致趋势，数据量增加性能改善，验证了方法的可扩展性。
  - 错误分析分类合理，并指出训练后错误减少主要来自字段定位和模式对齐，而非保守预测。
  - 总体实验设计客观、充分，但缺少更多消融实验（如仅用通用生成 vs 仅用错误驱动生成的对比）。

## 6. 主要结论与发现
- SAYRE 框架生成的合成数据可**持续改进不同规模的 LMM**，在设备端模型中取得**总体最优结果**。
- 在开放类别 KIE 上提升尤其显著，表明合成数据增强了模型对未见模式和场景的泛化能力。
- 数据数量与性能呈**正向扩展关系**，小模型（2B）获益更大，说明合成数据对紧凑模型特别有效。
- 错误分析显示，合成训练**减少字段级错误 17.8%**，主要改善在于密集表格、业务标识符和合同条款等复杂结构，说明模型学到的是模式感知提取而非简单文本识别。

## 7. 优点
- **免手工模板**：仅需少量示例即可自动合成，可扩展至多种文档类别。
- **错误驱动生成**：将真实失败案例转化为困难样本，弥补通用生成对失败模式覆盖不足的缺陷。
- **多智能体设计**：感知与生成分工明确，兼顾内容、布局与多样性（通过 persona card）。
- **视觉退化模拟**：合成图像叠加光学噪声，提升对实际采集条件的鲁棒性。
- **严格实验验证**：在权威基准 UniKIE 上系统对比，包含可扩展性与错误分析，结论可靠。
- **开源与可复现**：虽未直接提供代码，但方法描述详细，基于公开模型与工具。

## 8. 不足与局限
- **依然无法处理手写内容**：SAYRE 目前无法可靠合成手写文本和其视觉变异，导致在混合打印/手写文档上性能下降（文中已明确提及）。
- **仅基于 Qwen3-VL 系列**：虽然实验证明了在 Qwen3 上的有效性，但在其他架构（如 InternVL、MiniCPM）上的泛化能力未经测试。
- **合成数据量固定为 1M**：未讨论更大量级（如 10M+）下的性能上限或过拟合风险。
- **缺乏消融实验**：未单独分析通用生成和错误驱动生成的各自贡献，也未分析不同感知代理配置或 persona card 数量影响。
- **部署成本**：合成框架本身依赖强大服务器端模型（Qwen-VL-Max、Qwen3-Max 等），用户需要额外算力生成数据，未报告生成 1M 样本的资源开销。
- **领域覆盖有限**：实验集中在常见商业文档（发票、收据、合同等），未涉及医疗、法律、科学文献等更专业化文档类型。
- **评估指标单一**：仅采用字段级 F1，未评估在线端到端延迟、内存占用等部署相关指标。

（完）
