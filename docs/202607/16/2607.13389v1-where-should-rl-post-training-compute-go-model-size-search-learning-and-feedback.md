---
title: "Where Should RL Post-Training Compute Go? Model Size, Search, Learning, and Feedback"
title_zh: RL后训练计算应投入何处？模型大小、搜索、学习和反馈
authors: "Patrick Wilhelm, Odej Kao"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13389v1"
tags: ["query:post-multi"]
score: 9.0
evidence: 强化学习后训练计算分配，适用于基础模型
tldr: RL后训练通常受固定FLOP预算约束，但预算在模型大小、搜索、学习和反馈之间的分配策略未明确。本文提出GRPO后训练的FLOP核算框架，将计算分解为rollout/搜索、策略更新/学习和奖励反馈评估。实验发现最优分配随模型大小、预算和奖励系统变化，且模型选择与训练分配耦合。RACE协议可帮助识别分配区域，结果建议论文应报告总FLOP及其分配细节。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.13389v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1406, \"height\": 1012, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13389v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1409, \"height\": 725, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13389v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1409, \"height\": 979, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13389v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1420, \"height\": 689, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.13389v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1238, \"height\": 280, \"label\": \"Table\"}]"
motivation: 研究RL后训练中，在固定计算预算下如何最优分配计算资源给模型大小、搜索、学习和反馈。
method: 提出FLOP核算框架分解GRPO后训练计算，并在LoRA微调Qwen2.5策略上探索分配前沿。
result: 最优分配随模型大小、预算、奖励系统和评估目标变化；大模型消耗更多per-token计算，减少更新或rollout。
conclusion: 论文应报告总FLOP及其分配，RACE协议可辅助识别分配策略但非保证改进。
---

## 摘要
强化学习后训练越来越多地被用于调整基础模型，以进行推理、规划和基于反馈的机器人学习流程，但受限的后训练资源通常被简化为单一的总FLOP预算。我们研究了这一实践背后的固定预算决策问题：在相同的后训练预算下，应该使用更大的策略模型、训练更小的策略模型更长时间、生成更多的展开搜索，还是将计算投入到更强的奖励反馈上？我们引入了一个用于GRPO后训练的FLOP核算框架，将计算分解为展开/搜索、策略更新/学习以及奖励或反馈模型评估。在采用LoRA适配的Qwen2.5策略中，我们发现了条件分配边界：最佳观察分配随模型大小、计算预算、奖励系统和评估目标而变化。相同FLOP的模型大小比较表明，模型选择和训练分配是耦合的，因为更大的策略模型消耗更多的每token计算量，因此在相同预算下只能进行更少的更新或展开。奖励系统也会改变核算：基于规则的奖励将几乎所有非更新计算用于策略展开，而PRM风格的反馈则将预算的显著部分分配给奖励模型推理。我们提出RACE作为一种诊断性试点网格协议，而非保证改进的承诺，用于在昂贵的验证运行之前识别分配机制；我们的结果表明，RL后训练论文应报告总FLOPs以及计算如何在模型大小、搜索、学习和反馈之间分配。

## Abstract
Reinforcement Learning (RL) post-training is increasingly used to adapt foundation models for reasoning, planning, and feedback-driven robot-learning pipelines, but constrained post-training resources are often summarized by a single total FLOP budget. We study the fixed-budget decision problem behind this practice: under the same post-training budget, should one use a larger policy, train a smaller policy longer, generate more rollout search, or spend compute on stronger reward feedback? We introduce a FLOP-accounting framework for GRPO post-training that decomposes compute into rollout/search, policy-update/learning, and reward- or feedback-model evaluation. Across LoRA-adapted Qwen2.5 policies, we find conditional allocation frontiers: the best observed allocation changes with model size, compute budget, reward system, and evaluation target. Same-FLOP model-size comparisons show that model choice and training allocation are coupled because larger policies consume more per-token compute and therefore buy fewer updates or rollouts under the same budget. Reward systems also change the accounting: rule-based rewards spend nearly all non-update compute on policy rollouts, while PRM-style feedback allocates a visible part of the budget to reward-model inference. We present RACE as a diagnostic pilot-grid protocol, not a guarantee of held-out improvement, for identifying allocation regimes before expensive validation runs; our results suggest that RL post-training papers should report total FLOPs together with how compute is divided among model size, search, learning, and feedback.

---

## 论文详细总结（自动生成）

# 论文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在强化学习（RL）后训练中，固定总计算预算（总FLOPs）的情况下，应该如何分配计算资源到四个维度：模型大小（更大策略模型 vs 更小策略模型）、搜索（更多展开rollout）、学习（更多策略更新步数）以及反馈（更强但更昂贵的奖励反馈，如过程奖励模型PRM）？
- **研究动机**：当前RL后训练通常仅报告总FLOPs，但相同的总FLOPs可能对应完全不同的计算分配（例如，大模型少更新 vs 小模型多更新；规则奖励 vs 学习反馈模型）。这种不透明性会误导资源分配决策，尤其在机器人学习和基础模型微调中，反馈模块的成本不容忽视。
- **整体含义**：RL后训练的固定预算分配是一个受约束的资源分配问题，最佳分配取决于模型大小、预算、奖励系统和评估目标，不能仅用总FLOPs概括。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：提出一个FLOP核算框架，将后训练总计算分解为三项：
  \[
  C_{\text{total}} = C_{\text{search}} + C_{\text{learning}} + C_{\text{reward}}
  \]
  - \(C_{\text{search}}\)：自回归rollout生成计算（与采样数量、完成长度正相关）。
  - \(C_{\text{learning}}\)：策略优化计算（与更新步数、可训练参数、反向传播成本正相关）。
  - \(C_{\text{reward}}\)：奖励/验证模型推理计算（对规则奖励可忽略，对PRM式反馈显著）。
- **更新分数 ρ**：核心分配变量：
  \[
  \rho = \frac{C_{\text{learning}}}{C_{\text{search}} + C_{\text{learning}} + C_{\text{reward}}}
  \]
  小ρ表示搜索/奖励密集型，大ρ表示更新密集型。
- **LoRA微调中的计算核算**：考虑可训练适配器参数和冻结骨干网络的前向/激活成本，定义有效更新分数 \(f_{\text{eff}} = a + (1-a)f_{\text{LoRA}}\)（a=0.85）。
- **RACE诊断协议**：Reward-Aware Compute Allocation（RACE）——在有限试点预算下通过小规模IsoFLOP网格，拟合局部奖励-分配响应（默认二次模型），识别最佳分配区域，然后推荐验证运行配置。若拟合不稳定或边界主导，则回退到试点最优。RACE是诊断性协议，并非保证改进。
- **核心思想总结**：将总FLOPs分解为可操作的三部分，使分配策略透明化。

## 3. 实验设计：数据集/场景、基准、对比方法

- **数据集/场景**：
  - 训练：**Polaris-53K** 数学推理数据集。
  - 验证/下游评估：**GSM8K** 和 **MATH-500**（各250个样本），使用精确最终答案准确率、符号等价准确率、pass@1。
- **模型与设置**：
  - 策略模型：Qwen2.5-Instruct（1.5B、3B、7B），使用LoRA适配。
  - 奖励系统：五种——**稀疏**（二值正确性）、**结构化**（正确性+格式+答案形状）、**密集**（部分信用）、**代理PRM**（过程+结果信号）、**真实PRM**（Qwen2.5-Math-PRM-7B）。
  - 基础设置：K=2个采样完成，最大完成长度L=2048。
- **对比方法**：不同模型大小、不同奖励系统、不同更新步数/秩组合，在同一计算预算带（三个IsoFLOP带：C1p1e16、C1p8e16、C2p4e16）内比较ρ变化。
- **实验阶段**：
  - Stage A：模型大小（1.5B/3B/7B）× 多种LoRA秩（8-128）× 三个预算带 → 搜索-学习分配前沿。
  - Stage B：固定3B策略，比较四种奖励系统（稀疏、结构化、密集、代理PRM）× 9种秩 × 3预算带（共108次运行）。
  - Stage D：固定3B策略，使用真实PRM（Qwen2.5-Math-PRM-7B）× 9种秩 × 3预算带（共27次运行）。

## 4. 资源与算力

- **文中未明确说明具体GPU型号、数量或总训练时长**。
- **算力描述**：通过FLOP核算框架估算计算量（如总FLOPs量级为10^16），但硬件信息缺失。仅提到“同FLOP预算下大模型消耗更多per-token计算”，未提供实际硬件部署细节。

## 5. 实验数量与充分性

- **实验数量**：
  - Stage A：多模型大小（3种）× 多种秩（9种以上）× 3预算带 → 约100+次运行。
  - Stage B：108次运行（3预算带×9秩×4奖励）。
  - Stage D：27次运行。
- **充分性评估**：
  - **优点**：覆盖了模型大小、奖励类型、预算水平三个主要变量，且通过下游任务评估，实验设计系统。
  - **不足**：仅使用数学推理任务（单一领域）；仅使用Qwen2.5模型家族和LoRA适配；仅使用GRPO算法（未与其他RL算法比较）；种子数量有限（未明确报告随机种子的多次重复）。奖励系统对比缺乏对真实人类反馈的模拟。部分实验（如RACE验证）属于诊断性，非严格消融。

## 6. 论文的主要结论与发现

- **固定预算下存在结构化分配行为**：搜索-学习-奖励计算平面内，奖励随分配位置变化，存在可测量的响应曲面，而非仅取决于总FLOPs。
- **分配前沿是模型大小和奖励条件的**：
  - 模型大小耦合：同预算下，大模型每token计算成本高，导致更少的rollout和更新步数，最佳分配点随模型大小变化。
  - 奖励系统影响：规则奖励（稀疏、结构化、密集）的更新分数高（ρ≈0.72），PRM式反馈（代理PRM、真实PRM）的ρ较低（≈0.44-0.45）——因为奖励模型推理消耗了预算。
- **下游评估目标会改变首选分配**：
  - 原生训练奖励：规则奖励偏好高ρ，PRM偏好低ρ。
  - Pass@1符号准确率：通常偏好高ρ（a180设置）。
  - 通用评判器（Common Judge）分数：在密集、代理PRM、真实PRM下，偏好中等ρ（a100设置）。
  - 目标依赖：不同评价指标选择不同的最佳分配，不能混用。
- **RACE诊断协议**：可以在小网格中正确识别高更新/低更新区域（内网格和留出秩验证的准确率为1.0），但不能保证全局改进（奖励遗憾非零）。建议用于筛选候选配置，而非替代下游验证。

## 7. 优点

- **概念创新**：首次系统地将RL后训练的总FLOPs分解为搜索、学习、奖励三个维度，揭示分配问题的重要性。
- **方法论清晰**：提出核算框架和ρ变量，使分配策略可度量、可比较。
- **实验设计全面**：覆盖模型大小、奖励系统、预算水平三个关键变量，并包含下游多指标评估。
- **目标依赖性分析**：明确指出不同评价指标（原生奖励、pass@1、评判器分数）可能冲突，强调报告时应注明优化目标。
- **RACE协议实用**：作为低成本的诊断工具，可帮助减少昂贵验证运行次数，具有实际应用价值。

## 8. 不足与局限

- **领域受限**：仅使用数学推理作为代理任务，未直接验证机器人学习或真实环境。
- **模型/算法单一**：仅Qwen2.5系列、仅GRPO算法、仅LoRA微调，通用性待验证。
- **硬件/真实计算成本缺失**：未报告GPU型号、数量、训练时长，难以复现或比较效率。
- **统计可靠性有限**：未报告多次随机种子运行的结果，可能受噪声影响；FLOPs核算基于估计，未考虑硬件效率差异。
- **RACE局限性**：RACE是诊断协议，不保证提升；响应模型拟合不稳定时回退策略简单，可能错过更优配置。
- **奖励系统对比的简化**：PRM使用固定7B模型，未探索不同大小PRM的影响；规则奖励的区分度可能不足。
- **下游评估样本量小**：GSM8K和MATH-500仅使用250样本，可能影响统计显著性。
- **未探索更复杂的搜索策略**（如树搜索、自我一致性），仅固定rollout数量和长度。

（完）
