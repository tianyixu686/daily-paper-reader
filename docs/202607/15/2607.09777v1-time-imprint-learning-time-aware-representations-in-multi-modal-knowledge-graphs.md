---
title: "Time Imprint: Learning Time-Aware Representations in Multi-Modal Knowledge Graphs"
title_zh: 时间印记：在多模态知识图谱中学习时间感知表示
authors: "Pengyu Zhang, Klim Zaporojets, Congfeng Cao, Jia-Hong Huang, Paul Groth"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.09777v1"
tags: ["query:mr"]
score: 9.0
evidence: 多模态知识图谱中的时间感知表示学习
tldr: "多模态知识图谱中实体区分困难，时间信息可辅助但存在稀疏性和多时间戳问题。提出Time Imprint框架，将时间视为独立模态，通过三视图对比学习对齐时间、文本和视觉表示，并利用注意力池化聚合选定时间戳。在三个基准上链接预测Hits@1提升最高6.07%，模糊样本提升58%。该方法仅增加少量训练开销，揭示了时间作为模态的有效条件。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.09777v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 874, \"height\": 601, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09777v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1825, \"height\": 1034, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09777v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 890, \"height\": 246, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09777v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1794, \"height\": 455, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09777v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 881, \"height\": 698, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09777v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 881, \"height\": 702, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.09777v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1496, \"height\": 1079, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09777v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1551, \"height\": 290, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09777v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 850, \"height\": 494, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09777v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1524, \"height\": 343, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09777v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 642, \"height\": 449, \"label\": \"Table\"}]"
motivation: 现有方法忽视时间作为模态，难以处理时间语义稀疏和多时间戳引起的噪声。
method: 提出Time Imprint，通过三视图对比学习对齐时间、文本和视觉表示，并采用紧凑子集选择与注意力池化生成鲁棒时间嵌入。
result: "在三个MMKG基准上链接预测Hits@1提升最高6.07%，模糊样本提升58%。"
conclusion: 时间作为模态有效缓解实体模糊，且仅增加适度训练开销。
---

## 摘要
多模态知识图谱通过文本和图像等多种模态丰富实体，但具有高度相似多模态特征的实体仍然难以区分。实体的时间信息可作为额外模态来消除此类实体的歧义，但现有方法很少将时间视为与文本和图像并列的独立模态，这主要是由于两大挑战：(1) 稀疏的时间语义，阻碍了与更丰富模态的对齐；(2) 多个时间戳，在表示学习中引入噪声或降低鲁棒性。为应对这些挑战，我们提出时间印记框架，将时间视为实体级模态，并通过三视图对比目标联合对齐时间、文本和视觉表示。此外，为缓解多时间戳的歧义性，时间印记研究了紧凑的时间戳子集选择设计空间，并通过注意力池化将所选时间戳聚合为判别性时间嵌入，平衡时间特异性和鲁棒性。在三个MMKG基准上的实验表明，时间印记实现了最先进的链接预测性能，整体上Hits@1最高提升6.07%，并在前1%歧义样本子集上提升高达58%。我们进一步考察了不同融合策略以及对时间戳可用性和质量的敏感性，阐明了时间作为模态在何时以及为何最有益，同时仅增加适度的训练开销。我们在 https://anonymous.4open.science/r/Time-Imprint 公开代码。

## Abstract
Multi-Modal Knowledge Graphs (MMKGs) enrich entities with multiple modalities such as text and images, yet entities with highly similar multi-modal features remain difficult to distinguish. Temporal information of an entity can serve as an additional modality to disambiguate such entities, but existing approaches rarely treat time as a separate modality alongside text and images due to two major challenges: (1) sparse temporal semantics, which hinder alignment with richer modalities, and (2) multiple timestamps, which introduce noise or reduce robustness in representation learning. To address these challenges, we propose Time Imprint, a framework that treats time as an entity-level modality and jointly aligns temporal, textual, and visual representations via a three-view contrastive objective. Additionally, to mitigate multi-timestamp ambiguity, Time Imprint studies a compact timestamp subset selection design space and aggregates the selected timestamps into a discriminative temporal embedding with attention pooling, balancing temporal specificity and robustness. Experiments on three MMKG benchmarks demonstrate that Time Imprint achieves state-of-the-art link prediction performance, improving Hits@1 by up to 6.07\% overall and yielding up to 58\% gains on the subset of the top-1\% ambiguity samples. We further examine different fusion strategies and the sensitivity to timestamp availability and quality, clarifying when and why time-as-modality is most beneficial, while adding only modest training overhead. We release our code at https://anonymous.4open.science/r/Time-Imprint.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：多模态知识图谱（MMKG）通过整合文本、图像等模态丰富实体表示，广泛应用于推荐、搜索等Web应用。然而，当不同实体共享高度相似的文本和视觉内容时，模型难以区分这些实体。
- **核心问题**：现有方法通常将时间信息作为辅助标签用于时序知识图谱补全（如四元组扩展），而文本和图像在实体级表示中建模，导致时间与其他模态在表示层面不匹配。论文提出将时间作为独立的实体级模态，与文本和图像对齐，以解决两个关键挑战：
  1. **稀疏时间语义**：时间单独不足以刻画实体，语义强度弱于文本和图像，难以对齐。
  2. **多时间戳歧义**：实体常关联多个时间戳（如创作、发布、获奖），全部纳入会引入噪声，单时间戳则缺乏鲁棒性。
- **动机**：利用时间信息消除高度相似实体的歧义（例如“拿破仑·波拿巴”与电影《拿破仑(2023)》，其时间不同而图文相似）。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：提出 **Time Imprint** 框架，将时间视为实体级独立模态，通过三视图对比学习对齐时间、文本和视觉表示；并采用紧凑子集选择与注意力池化生成鲁棒时间嵌入。
- **关键技术细节**：
  - **模态编码器**：
    - **时间编码器**：将年份通过正弦编码映射为稠密向量（式1），含可学习频率和相位参数。
    - **多时间戳选择与注意力池化**：为每个实体从候选时间戳集合中选择最多 \(K\) 个离中心统计量（中位数）最近的时间戳，并强制包含最早年份（锚约束）。然后通过余弦相似度注意力池化（式3）聚合为单一时间嵌入 \(\hat{\mathbf{e}}^{\text{time}}_e\)，温度控制注意力分布尖锐度。
    - **图像与文本编码器**：分别使用BEiT和BERT编码为离散token，选取最频繁token，投影层可训练，嵌入表冻结。
  - **时间感知实体编码器**：
    - 将聚合时间嵌入线性投影为时间前缀 token \(\mathbf{t}_e\)（初始化为零）。
    - 构建输入序列：[ENT], \(\mathbf{t}_e\), 结构嵌入 \(\mathbf{s}_e\), 视觉token, 文本token。通过多层Transformer编码，输出[ENT]位置的表示作为统一实体表示 \(\mathbf{e}\)（式6）。
  - **时间感知评分**：
    - **上下文解码**：对查询 \((h, r, ?)\) 通过Transformer解码器获得上下文嵌入 \(\mathbf{c}\) 和上下文关系嵌入 \(\tilde{\mathbf{r}}\)。
    - **时间注入**：通过关系感知门控（式8）将头实体时间嵌入注入到 \(\mathbf{h}'\)；再通过关系调制（式9）将已知实体时间嵌入融入关系嵌入 \(\tilde{\mathbf{r}}'\)。
    - **Tucker评分**：使用Tucker分解（式10）计算候选得分。
    - **链接预测损失**：softmax交叉熵（式11）。
  - **多视图对比对齐**：为每个实体构建五个视图（[ENT]表示、全局均值池化、视觉均值池化、文本均值池化、时间嵌入 \(\hat{\mathbf{e}}^{\text{time}}_e\)），采用InfoNCE损失（式12）对齐视图。时间视图参与所有配对，并加权因子 \(\gamma\)。
  - **联合训练目标**：\(\mathcal{L} = \mathcal{L}_{\text{lp}} + \lambda \mathcal{L}_{\text{con}}\)（式13）。
- **算法流程**：① 提取时间戳、文本、图像 → ② 模态编码生成token和时间嵌入 → ③ 构建输入序列，Transformer编码生成实体表示 → ④ 时间感知评分 → ⑤ 多视图对比对齐 → ⑥ 联合损失优化。

## 3. 实验设计：数据集、基准、对比方法

- **数据集**：三个广泛使用的MMKG基准：
  - **DB15K**、**MKG-W**、**MKG-Y**。每个数据集提供实体、结构化三元组、文本描述和关联图像。从38,961个实体（共42,842，覆盖率90.94%）中提取时间戳，平均每个实体3.72个候选年份；手动审计100个实体，89%含至少一个正确年份。
  - 时间戳提取来源：实体文本描述中的年份（规则提取）以及图像元数据中的拍摄/上传日期；统一采用年份粒度，有方法学论证。
- **基准**：
  - 标准过滤链接预测协议（filtered setting），指标：MRR、Hits@1、Hits@3、Hits@10。
- **对比方法**：
  - **知识图谱嵌入方法**：TransE, DistMult, ComplEx, RotatE, TuckER。
  - **多模态知识图谱补全方法**：IKRL, TBKGC, TransAE, MMKRL, RSME, VBKGC, OTKGE, MACO, IMF, QEB, VISTA, AdaMF, MANS, MMRNS, NativE, MyGO, M-REFT, SNAG, MOMOK。
  - 实现基于MyGO框架（https://github.com/zjukg/MyGO）。
- **模糊子集实验**：从每个数据集中选取前1%歧义样本（约150个实体），定义为预训练文本和图像嵌入空间中最近邻余弦相似度最高的实体。与最强多模态基线SNAG和MOMOK比较。

## 4. 资源与算力

- 论文明确说明：所有实验在 **单个 NVIDIA H100 GPU** 上进行。
- 训练配置：优化器Adam，batch size 1024，嵌入维度256，训练 **1500 个epoch**。
- 未提及训练时间的具体小时数，但指出仅增加**适度的训练开销**。

## 5. 实验数量与充分性

- **实验组数**：涉及多个维度，具体包括：
  - **整体性能**：三个数据集上的全指标对比（表1），约20+基线。
  - **模糊子集**：三个数据集上的对比（表2），对比SNAG和MOMOK。
  - **消融实验**（表3）：五种变体（基线、禁用时间token、禁用评分注入、禁用对比、全模型），每个数据集四个指标，共5×3×4=60个数据点。
  - **多时间戳建模**：策略选择（6种策略，表4与图4）、数量 \(K\)（1-10，图5）、聚合方式（softmax/sparsemax/entmax 1.5，表5），每个数据集均有报告。
  - **鲁棒性噪声**（图6）：5个污染率 × 3数据集 × 4指标，共60个数据点。
- **评估充分性与公平性**：
  - 遵循标准过滤协议，确保结果可复现。
  - 模糊子集的选取定义清晰，仅与最相关同级基线比较（SNAG和MOMOK），避免冗余对比。
  - 消融实验逐一移除组件，并说明“部分移除可能更差”的交互效应，分析深入。
  - 超参数（如 \(K=6\), 中位数选择+最早锚）通过实验确定，并报告敏感性。
  - 缺陷：未进行跨数据集参数调优的全局最优验证；噪声实验仅测试随机噪声一种类型，未考虑结构化噪声（如时间顺序错乱）；未评估时间戳缺失的情况（仅污染）。

## 6. 论文的主要结论与发现

1. **整体性能**：Time Imprint 在三个基准上达到最先进水平，尤其在MKG-W上Hits@1提升6.07%，MRR提升4.53%；在DB15K和MKG-Y上也取得最佳或竞争结果。
2. **歧义样本显著改进**：在前1%歧义子集上，MKG-W Hits@1提升58.21%，三个数据集上均有大幅提升，证明时间作为模态在图文模糊时价值最大。
3. **多时间戳建模**：中位数选择+最早锚策略最优；性能随选取数量 \(K\) 呈现倒U形，最优在 \(K=4-6\)；软注意力（softmax）最稳定。
4. **鲁棒性**：在40%以下随机噪声时性能下降平缓；100%噪声时近似退化到无时间基线，模型自动忽略噪声。
5. **三个时间注入组件互补**：评分注入最关键（去除降幅最大），但三组件需同时激活才能达到最佳，部分激活可能因不一致而更差。

## 7. 优点：方法或实验设计上的亮点

- **方法创新性**：
  - **首次系统地将时间作为独立实体级模态**与文本、图像对齐，而非仅三元组级标签。
  - **多时间戳紧凑选择与注意力池化**：兼顾特异性和鲁棒性，考虑了极值锚定。
  - **三视图对比对齐**：鼓励稀疏时间嵌入与稠密模态对齐，克服时间语义稀疏。
  - **时间注入三阶段**：编码器、评分、对比，提供互补增益。
- **实验设计的完整性**：
  - **全面消融**：不仅分析组件贡献，还揭示交互效应（部分移除可能劣于无时间）。
  - **歧义子集分析**：直接验证时间作为模态的核心动机，提升可解释性。
  - **多时间戳设计空间系统性探索**：策略、数量、聚合方式。
  - **鲁棒性噪声测试**：定量揭示模型退化模式。
- **实用性与可复现**：代码开源，训练仅需单张H100，开销适度。

## 8. 不足与局限

- **时间粒度限制**：仅使用年份粒度，可能丢失月份/日级别的区分信息。论文虽论证年覆盖广泛且月份带来的增益边际（<1%实体对），但未在实验中验证更细粒度是否带来额外提升。
- **时间戳提取依赖**：时间戳从文本和图像元数据中规则提取，可能存在噪声和遗漏（论文手动审计89%正确，仍有11%错误）。未设计去噪学习或置信度机制。
- **噪声类型单一**：鲁棒性仅测试随机均匀噪声，未评估结构化干扰（如偏移、时序颠倒）或缺失时间戳场景，实际应用可能面对更复杂噪声。
- **组件依赖性强**：三个时间注入阶段需同时激活方达最优，部分移除效果更差，说明模型对此设计敏感，替代设计空间未探索。
- **模糊子集定义局限**：仅基于文本和图像嵌入的余弦相似度定义歧义，未考虑与时间戳的关系或三元组结构歧义。
- **计算与部署开销**：虽然只增加适度训练开销，但模型包含Transformer编码器和解码器，推理时需为每个候选实体计算实体表示，大规模场景可能成为瓶颈（论文未分析推理耗时）。
- **可扩展性验证不足**：仅在三个中等规模数据集（DB15K、MKG-W、MKG-Y）上验证，未在更大MMKG（如Wikidata）或跨领域数据集上测试。
- **未考虑时间动态性**：时间作为静态属性而非时序演化关系，未建模时间顺序和因果关系，限制了在时间推理任务上的适用性。

（完）
