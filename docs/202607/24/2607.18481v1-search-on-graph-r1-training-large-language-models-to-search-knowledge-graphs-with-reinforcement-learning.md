---
title: "Search-on-Graph-R1: Training Large Language Models to Search Knowledge Graphs with Reinforcement Learning"
title_zh: 基于图的搜索-R1：使用强化学习训练大语言模型以搜索知识图谱
authors: "Jia Ao Sun, Hao Yu, Fengran Mo, Zhan Su, Yuchen Hui, Bang Liu, Jian-Yun Nie"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.18481v1"
tags: ["query:mr"]
score: 8.0
evidence: 基于强化学习的多跳知识图谱问答导航内化
tldr: 知识图谱问答需从主题实体导航至远距离答案，现有方法依赖前沿LLM推理成本高。本文提出Search-on-Graph-R1，借助金SPARQL查询为教师提供路径支架，通过监督微调与强化学习将导航能力内化至8B模型。在WebQSP、CWQ、GrailQA上，8B模型超越所有对比的前沿LLM系统，并在CWQ上取得最优结果。该方法无需额外推理模块或LLM评判器，且强化学习相比监督微调显著减少搜索步数。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.18481v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1650, \"height\": 737, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18481v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 800, \"height\": 572, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.18481v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 812, \"height\": 539, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18481v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 604, \"height\": 272, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18481v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1381, \"height\": 1059, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18481v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 808, \"height\": 240, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18481v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 808, \"height\": 178, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18481v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 802, \"height\": 298, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18481v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1042, \"height\": 730, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18481v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 735, \"height\": 551, \"label\": \"Table\"}]"
motivation: 前沿LLM驱动的知识图谱问答推理成本高，需将导航能力高效内化至小模型。
method: 用金SPARQL查询为教师提供已知路径，结合监督微调与强化学习训练8B模型实时搜索知识图谱。
result: 在WebQSP、CWQ、GrailQA上超越所有前沿LLM系统，CWQ上取得最优结果，推理无需辅助模块。
conclusion: 监督微调与强化学习互补，强化学习减少搜索步数，方法可跨模型迁移且部署高效。
---

## 摘要
知识图谱问答（KGQA）需要从主题实体导航到相距若干关系的答案。最近的方法通过检索工具提示前沿LLM探索图谱，但它们依赖于前沿规模的推理，使得部署成本高昂。我们提出Search-on-Graph-R1（\sogrone{}），通过监督微调（SFT）后接强化学习（RL），将这种导航内化到一个紧凑的8B模型中。我们的核心思想是，用每个问题的黄金SPARQL查询来支撑一个前沿教师模型，使得教师模型使用实时的\texttt{Search}工具遍历已知的包含答案的路径，而不是自行发现路径。由于每次调用都针对实时的Freebase服务器执行，因此生成的轨迹在构造上就基于知识图谱。在WebQSP、CWQ和GrailQA上，\sogrone{}（8B参数）超越了比较中所有冻结的前沿LLM系统，并在CWQ上取得了我们比较的所有系统中最强的结果。它在推理时不使用任何辅助模块，在训练时不使用LLM评判器。分离每个训练阶段表明，SFT和RL贡献了互补的收益，我们的方法可以跨模型族迁移，并且RL学会比其SFT初始化使用更少的\texttt{Search}调用到达答案。

## Abstract
Knowledge graph question answering (KGQA) requires navigating from topic entities to an answer several relations away. Recent methods prompt a frontier LLM to explore the graph through a retrieval tool, but their reliance on frontier-scale inference makes them costly to deploy. We present Search-on-Graph-R1 (\sogrone{}), which internalizes this navigation into a compact 8B model through supervised fine-tuning (SFT) followed by reinforcement learning (RL). Our central idea is to scaffold a frontier teacher with each question's gold SPARQL query, so the teacher traverses a known answer-bearing path with a live \texttt{Search} tool rather than having to discover the path itself. Since every call executes against a live Freebase server, the resulting trajectories are grounded in the knowledge graph by construction. On WebQSP, CWQ, and GrailQA, \sogrone{} at 8B surpasses every frozen frontier-LLM system in our comparison and posts the strongest results on CWQ of any system we compare against. It does so using no auxiliary module at inference and no LLM judge during training. Isolating each training stage shows that SFT and RL contribute complementary gains, our approach transfers across model families, and RL learns to reach answers in fewer \texttt{Search} calls than its SFT initialization.

---

## 论文详细总结（自动生成）

好的，以下是根据您提供的论文内容生成的结构化、深入且客观的中文总结。

---

### 论文详细中文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：知识图谱问答（KGQA）需要模型从给定的主题实体出发，通过多跳关系导航到答案。现有方法通常依赖大规模、冻结的前沿LLM（如GPT-4）与知识图谱交互推理，但这类方法推理成本高、延迟大，难以实际部署。如何将复杂的KG导航能力高效地内化到一个小型、可部署的开源模型中，是本文要解决的关键问题。
- **研究动机**：将导航能力从大型教师模型迁移至紧凑学生模型，并通过强化学习进一步优化，从而在保持高性能的同时大幅降低部署成本。
- **整体含义**：提出一种训练方法，使得一个小型8B模型能够在推理时自主、高效地搜索知识图谱，无需依赖外部LLM评判器或辅助模块，实现成本与精度的良好平衡。

#### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：利用每个训练问题自带的黄金SPARQL查询为教师模型提供明确导航路径，让教师模型通过实时搜索工具（\texttt{Search}）遍历该路径，生成完全基于KG的完整轨迹。然后，学生模型（8B规模）先通过监督微调（SFT）学习这些轨迹，再通过强化学习（GRPO）进一步优化，最终实现自主导航。
- **关键技术细节**：
  - **SPARQL支撑的教师轨迹生成**：将每个问题的黄金SPARQL查询提供给235B规模的教师LLM（Qwen3-235B），教师模型将其理解为导航蓝图，逐步调用\texttt{Search}工具与真实Freebase知识图谱交互，记录工具调用和结果，形成包含最终答案的多轮轨迹。只保留最终答案完全匹配且所有搜索无错误的轨迹。
  - **批量邻居检索工具（Search）**：区别于之前每次只查询一个实体，新工具接受实体列表，一次性返回所有相邻三元组。这避免了模型因遗漏部分实体而丢失答案的情况，同时降低了总调用次数。
  - **监督微调（SFT）**：使用LoRA（低秩适应）在教师轨迹上微调学生模型，只计算助理轮次（工具调用和答案）的损失，不训练模型预测工具输出。
  - **强化学习（GRPO）**：在SFT基础上，以实时KG交互进行RL训练。奖励函数为：r(τ) = g(τ) · rem(τ) · f(n_t)。其中g(τ)是格式门控（至少一次工具调用且答案格式规范），rem(τ)是二元精确匹配奖励（只有完全正确才得1），f(n_t)是效率惩罚因子（对超出自由阈值Td的调用次数进行线性惩罚，但只作用于正确轨迹，不改变正确/错误轨迹之间的符号）。在RL中，模型通过与环境交互自行探索更有效的路径。
- **算法流程**：训练分为三个阶段：1）教师轨迹生成；2）学生SFT预热（得到SOG-SFT）；3）GRPO强化学习（得到SOG-R1）。推理时学生模型仅接收问题+主题实体，通过\texttt{Search}工具实现在线多步搜索。

#### 3. 实验设计：数据集、基准与对比方法
- **数据集**：三个标准Freebase KGQA基准——WebQSP、CWQ（Complex WebQuestions）、GrailQA。
- **评测指标**：精确匹配准确率（Hits@1）。
- **对比方法**：
  - **LLM-only基线**：不使用KG的IO提示、CoT、自一致性（基于同一8B骨干模型）。
  - **冻结LLM方法**：ToG、PoG、LMP、iQUEST、ORT、DoG、PARoG、Search-on-Graph（使用GPT-4或GPT-4o等前沿模型）。
  - **后训练LLM方法**：ChatKBQA、Reasoning-on-Graph、KG-Agent、RGR-KBQA、CompKBQA、D-RAG、KG-Hopper、Explore-on-Graph（EoG）。其中EoG和KG-Hopper是最直接的可比系统，均基于Llama-3.1-8B。
- **训练/验证划分**：每个数据集内，SFT和RL共用相同的已保留教师轨迹池；固定300个验证集。WebQSP使用全部2707条可用轨迹，CWQ使用8700条，GrailQA使用2700条。

#### 4. 资源与算力
- **硬件**：
  - SFT和RL训练：单个8×NVIDIA H200 141GB SXM节点（共1,128 GB聚合显存，NVLink连接）。
  - 教师轨迹生成：Qwen3-235B-A22B-Thinking-2507-FP8端点部署在单独的4×H100节点上，通过vLLM提供服务。
- **后端**：Freebase SPARQL使用OpenLink Virtuoso 7.2.14运行在Apptainer容器中。
- **训练时长**：文中未给出具体的各阶段时长（如wall-clock time），但提到了完整超参数表。

#### 5. 实验数量与充分性
- **实验数量**：
  - 主表（表2）对比了三个数据集上十余种基线方法。
  - 训练阶段消融（表3）：包含四种配置（Base、SOG-R1-ZERO（仅RL）、SOG-SFT（仅SFT）、SOG-R1（完整）），在三个数据集上报告均值±标准差（三个种子）。
  - 路径压缩分析（表4）：比较SFT和RL在正确回答上的平均搜索调用数，覆盖三个数据集。
  - 数据规模消融（图2）：在CWQ上使用5个不同训练池大小（2.7k至8.7k），报告SFT和完整管道性能。
  - 骨干通用性实验（表5）：在Qwen3-4B上重复完整管道，对比Llama-3.1-8B结果。
- **充分性与客观性**：
  - 实验设计全面：覆盖了不同方法族、不同骨干、不同数据量、不同训练阶段。
  - 消融清晰：分离SFT和RL贡献，并设RL-only比较点。
  - 控制了骨干匹配：对EoG和KG-Hopper使用相同骨干8B模型。
  - 统计报告：主要结果报告三个种子均值±标准差，表明可靠性。
  - 对比方法数据均来自论文报告的最佳配置，比较公平。
  - 但缺乏对超出Freebase的其他KG（如Wikidata）的实验，也未进行端到端实体链接测试（采用oracle标题实体），这可能会限制泛化性。

#### 6. 论文的主要结论与发现
- **主要性能**：SOG-R1（8B）在所有三个数据集上超越所有对比的冻结前沿LLM系统；在CWQ上取得所有后训练方法中最优结果（88.7%），在WebQSP上接近最佳（91.7% vs EoG 92.8%），在GrailQA上排名第二（90.0% vs EoG 92.1%）。
- **阶段贡献**：SFT提供最大的单阶段提升（比Base高+41.7/+54.1/+34.7个百分点），RL进一步改进（+0.8/+2.4/+1.2个百分点）；仅RL效果显著差于仅SFT，说明SFT冷启动是必要的。
- **路径压缩**：RL阶段学会了更高效的导航，在三个数据集上搜索调用数分别减少15%、4%、10%，同时准确率提升。
- **数据规模影响**：RL在小数据池上补偿明显（+7.8 EM at 2.7k），大数据池上SFT已很强，RL增益缩小；管道在约8.7k问题上达到瓶颈。
- **跨模型迁移**：方法可迁移至不同族及参数规模的模型（Qwen3-4B），同样观察到SFT+RL的增益模式。

#### 7. 优点：方法或实验设计上的亮点
- **创新性**：利用SPARQL查询作为教师导航的“脚手架”，而不是让教师自行发现路径，确保了轨迹的KG归因性且无需过滤。此策略充分利用了KGQA训练数据的结构化特性。
- **高效性**：使用批量搜索工具减少调用次数，且GRPO中的效率惩罚因子使模型自然学会短路径，不牺牲精度。
- **简洁性**：训练过程中不使用LLM评判器（避免模型依赖带来的偏差），推理时不使用任何辅助模块，完全端到端自洽。
- **透明性**：奖励函数完全确定（格式+精确匹配+效率因子），无随机性或外部依赖，利于复现。
- **实验严谨**：进行了充分的消融实验、数据规模实验和跨骨干验证，并报告了多种子结果标准差，展示了方法的稳健性。

#### 8. 不足与局限
- **KG局限性**：仅基于Freebase评估，未在Wikidata等其他KG上验证，且假设训练问题均带有黄金SPARQL查询（缺乏此类注释的KG无法直接使用）。
- **实体链接**：采用oracle实体链接（提供金标准主题实体），未与自动实体链接结合评估，实际部署性能可能会下降。
- **泛化性**：GrailQA中因类级问题（无主题实体）被过滤，降低了训练池可用性；方法对这类问题的处理未明确讨论。
- **数据依赖**：WebQSP的池子很小（2707条），尽管RL有所补偿，但整体性能上限受限于初始轨迹的质量和数量。
- **计算开销**：教师轨迹生成依赖235B模型和实时KG查询，仍需要一定计算成本；虽是一次性成本，但对资源有限的团队可能不友好。
- **缺少与更大规模后训练模型的比较**：未与Llama-3.1-70B或其他更大骨干的后训练模型对比（如EoG是否用更大模型？论文未提），难以判断8B模型的绝对性能天花板。

（完）
