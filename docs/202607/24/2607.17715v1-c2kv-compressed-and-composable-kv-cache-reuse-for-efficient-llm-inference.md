---
title: "C$^2$KV: Compressed and Composable KV Cache Reuse for Efficient LLM Inference"
title_zh: C$^2$KV：用于高效LLM推理的压缩与可组合KV缓存复用
authors: "Chuheng Du, Junyi Chen, Hanlin Tang, Kan Liu, Tao Lan, Lin Qu, Chaoyue Niu, Shengzhong Liu, Guihai Chen, Fan Wu"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.17715v1"
tags: ["query:mr"]
score: 8.0
evidence: 面向高效检索增强生成的压缩KV缓存重用
tldr: 长上下文LLM推理中，KV缓存重用面临存储访问瓶颈，现有方法忽略压缩导致高成本。本文提出C$^2$KV框架，通过学习位置无关的可组合压缩KV缓存流形，结合轻量Extractor和压缩-拼接协同训练，实现高效非前缀KV缓存重用。在长上下文基准上取得最高17倍推理加速，同时保持生成质量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17715v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 828, \"height\": 394}, {\"url\": \"assets/figures/arxiv/2607.17715v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 862, \"height\": 506}, {\"url\": \"assets/figures/arxiv/2607.17715v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 828, \"height\": 318}, {\"url\": \"assets/figures/arxiv/2607.17715v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 847, \"height\": 554}, {\"url\": \"assets/figures/arxiv/2607.17715v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1572, \"height\": 508}, {\"url\": \"assets/figures/arxiv/2607.17715v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 749, \"height\": 549}, {\"url\": \"assets/figures/arxiv/2607.17715v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1734, \"height\": 865}, {\"url\": \"assets/figures/arxiv/2607.17715v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 868, \"height\": 452}, {\"url\": \"assets/figures/arxiv/2607.17715v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1549, \"height\": 407}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.17715v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 881, \"height\": 502}, {\"url\": \"assets/tables/arxiv/2607.17715v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 848, \"height\": 364}, {\"url\": \"assets/tables/arxiv/2607.17715v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 802, \"height\": 448}, {\"url\": \"assets/tables/arxiv/2607.17715v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1666, \"height\": 282}, {\"url\": \"assets/tables/arxiv/2607.17715v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1752, \"height\": 1386}]"
motivation: 长上下文LLM推理中，现有KV缓存重用方法忽略存储访问瓶颈，导致高成本。
method: 提出C$^2$KV框架，学习位置无关的可组合压缩KV缓存流形，通过轻量Extractor和压缩-拼接协同训练实现灵活重用。
result: 在长上下文基准上，C$^2$KV显著降低KV缓存开销，实现最高17倍推理加速，保持生成质量。
conclusion: C$^2$KV为高效长上下文LLM推理提供统一框架，兼顾压缩与重用性能。
---

## 摘要
长上下文推理是现代大语言模型（LLM）应用（如检索增强生成和多文档推理）的核心。为了缓解日益增长的推理成本，最近的研究探索了键值（KV）缓存复用以减少冗余的预填充计算。然而，现有的复用方法主要关注计算节省，而忽视了长上下文LLM服务中的一个关键瓶颈：存储和访问大型KV缓存的成本。虽然KV压缩似乎是自然的补充，但将压缩与非前缀KV复用简单结合往往会导致严重的精度下降。在这项工作中，我们提出了C$^2$KV，一个用于非前缀KV复用的统一框架，它联合优化了KV提取和推理时拼接。C$^2$KV学习了一个可组合且可压缩的KV缓存流形，该流形被明确设计为位置无关的。我们的方法引入了一个轻量级的侧车提取器，带有可学习的压缩标记和结构化注意力流，从而实现了模块化的KV表示，可以在不修改冻结基础模型的情况下灵活地复用和拼接。我们进一步采用了压缩-拼接联合训练策略，以对齐提取时表示与其下游复用行为。在多个长上下文基准和模型系列上的大量实验表明，C$^2$KV显著降低了KV缓存存储和传输成本，在长上下文下实现了高达17倍的推理加速，同时保持了生成质量。

## Abstract
Long-context inference is central to modern large language model (LLM) applications such as retrieval-augmented generation and multi-document reasoning. To mitigate the growing inference cost, recent work has explored key-value (KV) cache reuse to reduce redundant prefill computation. However, existing reuse methods primarily focus on computation savings and overlook a critical bottleneck in long-context LLM serving: the cost of storing and accessing large KV caches. While KV compression appears to be a natural complement, naively combining compression with non-prefix KV reuse often leads to severe accuracy degradation. In this work, we propose C$^2$KV, a unified framework for non-prefix KV reuse that jointly optimizes KV extraction and inference-time concatenation. C$^2$KV learns a composable and compressed KV cache manifold that is explicitly designed to be position-agnostic. Our approach introduces a lightweight sidecar Extractor with learnable compression tokens and a structured attention flow, enabling modular KV representations that can be flexibly reused and concatenated without modifying the frozen base model. We further employ a compression-concatenation co-training strategy to align extraction-time representations with their downstream reuse behavior. Extensive experiments across multiple long-context benchmarks and model families demonstrate that C$^2$KV significantly reduces KV cache storage and transfer costs, achieving up to 17$\times$ inference speedup under long contexts, while preserving generation quality.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

长上下文推理是大语言模型（LLM）应用（如检索增强生成、多文档推理）的核心。现有KV缓存复用方法主要关注减少预填充计算（如Prefix Cache、CacheBlend等），但忽视了长上下文服务中的关键瓶颈：**存储和访问大型KV缓存的成本**。随着上下文长度增加，KV缓存大小线性增长，从DRAM到HBM的传输延迟成为主导因素。虽然KV压缩是自然解决方案，但简单地将压缩与非前缀KV复用结合会导致严重的精度下降，因为标准压缩表示是上下文耦合的、不可组合的。本文旨在解决这一矛盾，提出**C²KV**（Compressed and Composable KV Cache），通过联合学习压缩和可组合表示，实现高效的非前缀KV复用。

### 2. 论文提出的方法论：核心思想、关键技术细节

**核心思想**：学习一个**位置无关的可组合压缩KV缓存流形**，使得不同文档的压缩KV片段可以像“即插即用”一样在任意位置拼接，而不损失语义。

**关键技术细节**：

- **C2Extractor**：轻量级侧车模块，附着在冻结的基础LLM上。包含：
  - **C2Tokens**：可学习的压缩记忆槽。将文档token序列按压缩比k分块，每块分配一个C2Token（总共m = ⌈n/k⌉个）。C2Token共享相同初始嵌入向量。
  - **新的QKV投影头**：为C2Token独立训练每个Transformer层的Q、K、V投影矩阵，维度与基础模型一致。C2Token的QKV与原始token的QKV拼接后送入标准注意力。仅C2Token嵌入和投影头可训练（约增加10%参数量），基础模型冻结。
  - **残差均值池化变体（C²KV-Residual）**：在第一个Transformer层，将C2Token所属块内k个文档token的隐藏状态均值加到C2Token隐藏状态上，增强局部语义聚合。

- **结构化信息流（SIF）**：注意力掩码设计：
  1. **原始token不变性**：原始token只能关注其他原始token（标准因果掩码），不允许关注C2Token。
  2. **块局部语义提取**：每个C2Token只能关注自己块内的原始token及“沉没块”（第一个C2Token可关注系统提示等），防止跨块信息泄漏。
  3. **跨块因果累积**：C2Token可以因果关注前面的C2Token，使压缩表示能累积文档级上下文。

- **压缩-拼接联合训练**：在拼接多个文档的C2KV后进行生成，计算标准自回归语言建模损失（只对答案部分）。梯度只传播到C2Extractor参数。通过端到端监督，强迫提取器学习在不同组合下依然有效的表示。

- **推理时**：检索预计算的C2KV，应用新的位置嵌入（按拼接顺序重新分配RoPE），直接拼接，无需任何混合或重计算。

### 3. 实验设计：数据集、基准、对比方法

- **训练数据集**：HotpotQA、2WikiMultiHopQA、LongMagpie的训练集，共120k样本（各40k），构建多文档QA数据。
- **评估基准**：LongBench（7个子集：HotpotQA、2WikiMQA、MuSiQue、QMSum、Qasper、MultiNews、GovReport、SAMSum）和RULER（长上下文检索）。
- **对比方法**：
  - 上界：Full Recompute（FR）
  - 下界：Naïve Reuse（直接拼接）
  - 训练无方法：EPIC、CacheBlend
  - 训练基方法：Block-Attention（仅Llama3.1-8B）
  - 压缩基线：FR+SnapKV（4×压缩）
- **评估指标**：QA任务用F1分数，摘要任务用ROUGE-L；解码延迟用每token时间（TBT）；启动延迟用TTFT。

### 4. 资源与算力

文中未明确说明使用的GPU型号、数量及总训练时长。仅提供训练超参数：
- 优化器：AdamW，学习率5e-5，warmup比例0.06，权重衰减0.1
- 全局batch size：32，梯度累积步数：4
- 精度：BF16
- 训练轮数：1 epoch（120k样本）
- 模型规模：最大为Qwen3-14B，但训练成本未详述。

### 5. 实验数量与充分性

实验涵盖以下维度：
- **主实验**：TTFT-精度权衡（图7，3个模型 × 7个LongBench任务）、解码延迟随上下文长度变化（图8，3个模型 × 7种长度）
- **压缩比缩放**：固定压缩比（4×、8×、16×）及动态压缩比训练（表1，Llama3.1-8B上4个任务）
- **长上下文**：RULER评估（图9，上下文长度4k~64k，两种模型）
- **更大模型**：Qwen3-14B（表2，4个任务）
- **消融实验**：对比Anchor Token、无训练、固定位置、信息泄漏、全局信息等设计（表4，3个任务）
- **与压缩结合**：SnapKV与各复用方法组合的精度对比（表3，5个任务）
- **全精度比较**：附录表5完整结果。

**充分性**：实验覆盖多种模型家族（Qwen、Llama）、多种任务类型（QA、摘要、检索）、多种基线（训练无、训练基、压缩基线），消融充分。比较客观公平。

### 6. 论文的主要结论与发现

- **C²KV实现了最优的TTFT-精度权衡**：在几乎所有任务上，C²KV（4×压缩）的精度接近Full Recompute，而TTFT仅为加载压缩KV的时间，远低于其他方法。
- **显著降低解码延迟**：C²KV将有效KV缓存大小压缩4×以上，解码时每token时间随上下文增长平缓，在128k上下文下远低于其他方法。
- **压缩比优雅退化**：从4×到16×压缩，C²KV性能下降缓慢，动态压缩比训练可泛化到未见过的比例（如10×）。
- **通用性**：在Qwen3-4B、Llama3.1-8B、Qwen2.5-7B、Qwen3-14B上均有效，且不修改基础模型。
- **关键设计验证**：结构化信息流（SIF）和压缩-拼接联合训练不可或缺，Anchor Token或未训练提取器均导致严重精度下降。

### 7. 优点：方法或实验设计上的亮点

- **轻量级模块化**：C2Extractor作为侧车模块，不修改基础模型，可快速适配新模型（仅需训练约10%额外参数）。
- **同时实现压缩与复用**：统一框架解决存储带宽和计算冗余两大瓶颈，而非仅优化其一。
- **位置无关表示**：通过SIF和联合训练学习的位置无关KV表示，允许任意顺序拼接，避免混合重计算。
- **实验设计全面**：覆盖TTFT、解码延迟、压缩比例、模型规模、消融等多个维度，对比基线多样。
- **开源代码**：提供可复现的源码，增强可信度。

### 8. 不足与局限

- **仅支持离线文档复用**：假设可复用内容可预先提取，不支持在线或增量KV提取，在动态场景中受限。
- **均匀压缩比**：对所有文档和层使用固定压缩比，未考虑内容重要性或层敏感度，可能导致次优信息保留。
- **训练开销**：虽然只增加10%参数，但仍需对每个基础模型进行针对性的SFT训练，对快速迭代的开源模型不够灵活。
- **模型覆盖有限**：最大验证到14B参数，未在更大模型（如70B/130B）上测试，可扩展性待验证。
- **任务类型局限**：主要在多文档QA和摘要上评估，未涉及代码、数学推理等场景。
- **基线比较中Block-Attention仅限Llama3.1**：缺少在其他模型上的对比，公平性略有不足。

（完）
