---
title: QUBO-Optimized Evidence Selection for Retrieval-Augmented Question Answering with Unconventional Solvers
title_zh: 面向非常规求解器的检索增强问答中基于QUBO优化的证据选择
authors: "Rahul Singh, Madhav Vadlamani"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12334v1"
tags: ["query:mr"]
score: 8.0
evidence: 通过QUBO进行多跳问答的证据选择
tldr: 检索增强问答中的多跳问题需要选择互补证据，但传统top-k排序依赖单一相关性且不充分。本文提出将证据选择转化为二次无约束二进制优化（QUBO）问题，通过构建能量函数平衡相关性、需求覆盖、支持强度、冗余、互补性和紧凑性，最小化能量获取最优证据子集。在HotpotQA上，该方法与基于LLM的选择器性能相当，但避免了昂贵LLM中间推理，将组合优化与语义生成分离，为使用Ising/QUBO求解器高效处理证据选择提供了新路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12334v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1667, \"height\": 842, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.12334v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 639, \"height\": 434, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12334v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 711, \"height\": 174, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12334v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 645, \"height\": 356, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12334v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 610, \"height\": 319, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12334v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 751, \"height\": 261, \"label\": \"Table\"}]"
motivation: 现有RAG多跳问答中，top-k排序忽略证据互补，LLM选择器成本高且难扩展，需高效结构化选择方法。
method: 将证据选择建模为QUBO，构造能量函数融合相关性、需求覆盖、支持强度与紧凑性，利用非传统求解器求解最优子集。
result: 在HotpotQA上，QUBO选择器精确匹配和token-F1与LLM选择器相近，显著优于BM25、MMR等非LLM基线。
conclusion: 多跳证据选择可转为离散优化，LLM专攻语义生成，QUBO求解器高效管理上下文选择，降低整体推理成本。
---

## 摘要
检索增强问答依赖于选择共同支持答案生成的证据段落。然而，许多RAG流水线依赖于top-k排序，主要根据单个相关性分数选择段落，尽管多跳问题通常需要满足多个信息需求的互补证据。最近的基于LLM的选择器通过将检索视为集合选择来解决此问题，但在此中间阶段使用LLM成本高昂且难以扩展。在这项工作中，我们将证据选择表述为二次无约束二元优化（QUBO）问题。给定一个问题、候选段落和分解的信息需求，我们的方法构建了一个能量函数，该函数平衡相关性、需求覆盖、支持强度、冗余度、互补性和紧凑性。低能量解对应于覆盖所需需求同时避免不必要或重复上下文的紧凑证据子集。然后，所选段落被传递到下游语言模型进行答案生成，将组合证据选择与语义答案生成分离。我们在HotpotQA上评估了所提出的QUBO选择器，并将其与基于LLM的集合选择器以及非LLM基线（包括BM25、相关性top-k、最大边际相关性、混合词汇-语义排序、贪婪覆盖和随机选择）进行了比较。QUBO选择器在精确匹配和token-F1性能方面与基于LLM的选择器相当，同时为结构化证据选择提供了兼容求解器的公式。这些结果表明，多跳证据选择可以转化为离散优化，为RAG流水线开辟了一条道路，其中LLM保留用于语义处理和答案生成，而上下文选择由Ising/QUBO兼容求解器处理。

## Abstract
Retrieval-augmented question answering depends on selecting evidence passages that jointly support answer generation. However, many RAG pipelines rely on top-\(k\) ranking, where passages are selected mainly by individual relevance scores, even though multi-hop questions often require complementary evidence satisfying multiple information requirements. Recent LLM-based selectors address this by treating retrieval as set selection, but using an LLM for this intermediate stage can be costly and difficult to scale. In this work, we formulate evidence selection as a Quadratic Unconstrained Binary Optimization (QUBO) problem. Given a question, candidate passages, and decomposed information requirements, our method constructs an energy function that balances relevance, requirement coverage, support strength, redundancy, complementarity, and compactness. Low-energy solutions correspond to compact evidence subsets that cover the needed requirements while avoiding unnecessary or repetitive context. The selected passages are then passed to a downstream language model for answer generation, separating combinatorial evidence selection from semantic answer generation. We evaluate the proposed QUBO selector on HotpotQA and compare it with LLM-based set selectors and non-LLM baselines including BM25, relevance top-\(k\), maximal marginal relevance, hybrid lexical--semantic ranking, greedy coverage, and random selection. The QUBO selector achieves competitive exact-match and token-F1 performance relative to LLM-based selectors while providing a solver-compatible formulation for structured evidence selection. These results suggest that multi-hop evidence selection can be cast as discrete optimization, opening a path toward RAG pipelines where LLMs are reserved for semantic processing and answer generation, while context selection is handled by Ising/QUBO-compatible solvers.

---

## 论文详细总结（自动生成）

## 论文总结：QUBO-Optimized Evidence Selection for Retrieval-Augmented Question Answering with Unconventional Solvers

### 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：检索增强问答（RAG）在多跳问题（如HotpotQA）中通常依赖top‑k排序选择证据段落，仅考虑单个段落的相关性，忽略了段落之间的互补性、冗余性以及整体需求覆盖。近期基于LLM的集合选择器（如SetR）虽然能显式建模集合级目标，但需要多次调用LLM进行推理，成本高、延迟大、难以扩展。
- **背景**：随着RAG流水线规模增长，上下文选择成为瓶颈，亟需一种结构化、可并行、低能耗的优化方法。同时，量子退火、相干Ising机、数字退火器等非传统求解器擅长求解QUBO问题，因此将证据选择建模为QUBO问题，有望用专用硬件加速。

### 2. 论文提出的方法论

- **核心思想**：将证据选择转化为一个 **二次无约束二元优化（QUBO）** 问题。每个候选段落表示为一个二元变量，目标函数同时编码了：
  - **相关性**（passage relevance score）
  - **需求覆盖**（partial/direct support of information requirements）
  - **支持强度**（support strength weighting）
  - **冗余惩罚**（support‑aware pairwise redundancy）
  - **互补奖励**（pairwise complementarity）
  - **紧凑性**（size penalty）
  - **预算约束**（maximum number of passages）
  低能量解对应于满足所有信息需求且无冗余的紧凑证据子集。

- **关键技术细节**：
  - **预处理阶段**：使用LLM将原问题分解为M个信息需求（每需求有权重w_j）；为每个候选段落标注需求支持强度S_ji∈{0,1,2}。
  - **QUBO构建**：定义二元选择变量x_i，以及需求覆盖变量y^P_j, y^D_j。构造目标函数（式12），将约束通过二次惩罚项（式14）转化为无约束形式。
  - **算法流程**：
    1. 输入问题q → 生成信息需求R和候选段落集合P；
    2. 计算所需系数（相关性s_i、支持S_ji、冗余d_ik、互补b_ik等）；
    3. 将这些系数填入QUBO矩阵Q；
    4. 调用QUBO求解器（本文使用模拟退火，但可替换为量子/数字退火器等）得到最优解；
    5. 根据解选出段落，与问题拼接后送入LLM生成最终答案。
  - **模块化设计**：语义预处理（需求分解、支持标注）与组合优化（QUBO求解）分离，各自可独立替换。

### 3. 实验设计

- **数据集**：HotpotQA（distractor setting），每个example包含多跳问题、答案及干扰上下文。将每个titled context展平为句子级候选段落。
- **对比方法**：
  - **基于LLM的集合选择器**：SetR（给定需求后LLM直接选段落）、SetRO（LLM同时推断需求并选集）。
  - **非LLM基线**：BM25 top‑k、Relevance top‑k、MMR（基于相关性和BM25的两种变体）、混合词汇‑语义排序、贪婪覆盖、随机选择。
- **评估指标**：精确匹配（EM）、token‑level F1、需求覆盖率、平均选中段落数。
- **测试设置**：所有方法共享相同候选池和下游生成模型（Ollama 3.6:31b，即qwen3.6:35b），确保公平比较。

### 4. 资源与算力

- 论文**未明确说明所使用的GPU型号、数量或训练时长**。文中仅提到所有LLM组件（需求生成、支持标注、SetR基线、答案生成）均使用同一本地模型（Ollama 3.6:31b）。QUBO求解器采用模拟退火（经典算法），未提及专用硬件。实验规模较小（最大500样本），算力开销推测较低，但作者未提供具体资源信息。

### 5. 实验数量与充分性

- **实验组数**：
  - 开发集配置扫描（表1）：12种配置。
  - 与LLM选择器对比（表2）：在500个样本上测试。
  - 与非LLM基线对比（表3）：在100个样本上测试。
  - 消融实验：去除各目标组件的答案级消融（表4，100样本）和集合级消融（表5，100样本）。
- **充分性评估**：实验设计相对合理，覆盖了关键基线（LLM和非LLM）并进行了消融分析。但存在局限性：
  - 仅使用单数据集（HotpotQA），未在更多多跳QA或开放域QA上验证。
  - 测试样本量较小（最大500，最小100），统计稳定性可能不足。
  - 未提供重复实验的方差或置信区间。
  - 消融实验仅在100样本上进行，结论的泛化性有限。
  - 未在真实QUBO求解器（如量子退火器）上评估，仅用模拟退火替代。

### 6. 主要结论与发现

- **性能对比**：QUBO选择器在EM和F1上**与SetR/SetRO等LLM集合选择器非常接近**（差距<1%），且**显著优于**所有非LLM基线（BM25、MMR、随机等）。
- **集合特性**：QUBO选择器取得了**最高的需求覆盖率**，说明其显式优化需求覆盖的有效性。
- **消融结果**：
  - 相关性和需求覆盖对答案质量贡献最大。
  - 全QUBO目标虽未在EM/F1上达到最佳，但能产生更紧凑、更高边际效用的证据集，表明结构化优化有利于证据集质量。
- **模块化意义**：证明了多跳证据选择可以转化为离散优化问题，LLM可专注于语义理解和生成，而组合优化部分可由非传统求解器高效处理。

### 7. 优点

- **创新性**：首次将RAG证据选择显式建模为QUBO问题，为利用量子/经典非传统求解器加速RAG提供了可行路径。
- **模块化**：分离了语义预处理和组合优化，每个组件可独立替换或封装，适应不同硬件后端。
- **目标透明**：能量函数显式编码了多个集合级目标（覆盖、冗余、互补、紧凑），便于调试和扩展。
- **性能平衡**：无需LLM参与中间选择，仍能取得与LLM选择器竞争的结果，降低了推理成本。
- **可解释性**：QUBO解可以直接解释为每个段落的参与度，以及段落之间的相互作用。

### 8. 不足与局限

- **依赖LLM预处理**：需求分解和支持强度标注仍由LLM完成，无法完全摆脱LLM开销，且LLM标注质量可能影响QUBO系数准确性。
- **实验规模有限**：仅测试HotpotQA一个数据集，样本量小，未评估开放域或更大规模语料。
- **未与更先进的RAG方法对比**：如多轮检索、迭代检索、基于强化学习的集合选择等。
- **系数手动设定**：目标函数权重（α, β, γ等）通过网格扫描设定，缺乏自适应学习机制，难以直接推广到其他任务。
- **QUBO求解器实证缺失**：虽然论文主打“非常规求解器”，但实验仅用模拟退火，未在量子退火器、相干Ising机等真实硬件上验证，缺乏能耗和延迟对比。
- **缺乏统计显著性检验**：未报告置信区间或p值，无法判断性能差距是否显著。
- **扩展性担忧**：候选池规模增大时，QUBO矩阵规模和求解难度随之增长，文中未讨论大规模场景下的求解效率。

（完）
