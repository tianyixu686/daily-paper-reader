Title: Text-Prompted CLAP: Learning Query-Conditioned Audio Representations via Contrastive Learning

URL Source: https://arxiv.org/pdf/2607.25085v1

Published Time: Wed, 29 Jul 2026 00:16:51 GMT

Number of Pages: 5

Markdown Content:
# TEXT-PROMPTED CLAP: LEARNING QUERY-CONDITIONED AUDIO REPRESENTATIONS VIA CONTRASTIVE LEARNING 

# Mohan Li 1,2 , Rama Doddipatla 2, Philip C. Woodland 1

> 1

# Department of Engineering, University of Cambridge, Cambridge, UK 

> 2

# Toshiba Cambridge Research Laboratory, Cambridge, UK 

ABSTRACT 

Contrastive Language-Audio Pretraining (CLAP) learns aligned text and audio representations in a shared embedding space. However, independent encoding of each modality limits its ability to model cross-modal semantics in complex audio understanding and retrieval tasks. To address this limitation, this paper proposes Text-Prompted CLAP (TP-CLAP), a parameter-efficient extension of CLAP that in-troduces a cross-attention-based fusion module to incorporate tex-tual prompts into audio features. TP-CLAP is trained using an audio multiple-choice question (audio-MCQ) framework, where it learns to align query-conditioned audio representations with text embed-dings of correct answer choices via contrastive learning. Experi-ments demonstrate that TP-CLAP achieves competitive performance on audio question answering (audio-QA) with substantially larger audio-LLMs, while also improving the base CLAP model on audio– text retrieval and zero-shot classification benchmarks. The learned representations are further fine-tuned for attribute-focused audio-to-audio retrieval, showing that TP-CLAP consistently outperforms the standard CLAP baseline in music retrieval tasks. 

Index Terms — contrastive language-audio pretraining, query-conditioned audio representation, audio retrieval 

1. INTRODUCTION 

Learning shared representations across modalities is a fundamental objective in multimodal machine learning. A major breakthrough in this area was Contrastive Language–Image Pretraining (CLIP) [1], which demonstrated that large-scale contrastive learning [2] on paired image–text data can produce highly transferable representa-tions and enable zero-shot generalisation across various downstream tasks. Inspired by this advance, Contrastive Language–Audio Pre-training (CLAP) [3] extended the same paradigm to the audio do-main. By jointly training audio and text encoders to align semanti-cally matched audio–text pairs in a shared embedding space, CLAP established strong performance for audio–text retrieval and zero-shot audio classification [3, 4, 5, 6]. Despite its success, the standard CLAP framework is limited in its ability to perform query-dependent tasks due to its independent encoding of audio and text. A notable example is audio question answering (audio-QA), where the model must focus on aspects of the audio relevant to a given question, yet CLAP encodes audio without access to the query. A similar limitation arises in attribute-focused audio-to-audio retrieval, where the goal is to retrieve clips that share a specific attribute (e.g., instrument, mood, or acoustic environment) with the query audio rather than based on global similarity. In both scenarios, the static audio representations produced by CLAP cannot adapt to emphasise such context-sensitive information. Recent work has sought to address these challenges using pre-trained multimodal architectures [7, 8, 9]. While these methods de-liver promising results, they often rely on fine-tuning multimodal LLMs, leading to substantial computational cost during both train-ing and inference. This raises an important question: Can query-conditioned audio representations be learned while preserving the efficiency and scalability of CLAP? 

To this end, this work proposes Text-Prompted CLAP (TP-CLAP), a lightweight extension of CLAP that incorporates textual prompts into audio representations through a cross-attention-based fusion module. To achieve this, audio multiple-choice question (audio-MCQ) data [10] is leveraged by using questions as condi-tioning signals and training the model to align the fused embeddings with those of the correct answer choices. This approach remains compatible with the original CLAP objective while introducing minimal architectural overhead. In addition, the proposed method improves performance on standard audio–text retrieval and zero-shot classification benchmarks. Furthermore, the model is fine-tuned for attribute-focused audio-to-audio retrieval, enabling search driven by targeted characteristics rather than overall semantic similarity. The contributions of this paper are summarised as follows: • TP-CLAP, a text-prompted variant of CLAP, is proposed to produce query-conditioned audio representations via cross-modal attention. • An audio-MCQ-based supervision framework is introduced to learn such representations without requiring multimodal LLMs. • TP-CLAP is shown to be effective for query-dependent au-dio understanding and retrieval while also improving perfor-mance on conventional audio–text benchmarks. 

2. PROPOSED METHOD 2.1. Standard CLAP Model 

CLAP jointly trains an audio encoder Ea(·) and a text encoder Et(·)

that map an audio clip xa and a text description xt into a common multimodal embedding space: 

ea = Ea(xa), et = Et(xt), (1) Given a batch of N paired audio–text samples, CLAP is trained us-ing a symmetric InfoNCE loss to enforce bidirectional alignment: 

LCLAP = − 12N

> N

X

> i=1

"

log exp( s(eia, eit)/τ )

PNj=1 exp( s(eia, ejt )/τ )+ log exp( s(eit, eia)/τ )

PNj=1 exp( s(eit, eja)/τ )

#

,

(2) arXiv:2607.25085v1 [eess.AS] 27 Jul 2026 Fig. 1 . Architecture of the proposed TP-CLAP model. where s(·) denotes cosine similarity and τ is a learnable temperature parameter to penalise hard negative examples. At inference time, cross-modal retrieval and zero-shot classifi-cation are performed by computing the cosine similarity between query and candidate embeddings. While this formulation is effective for learning general-purpose audio and text representations, the two modalities remain encoded independently throughout the process. Consequently, the resulting audio embeddings are query-agnostic and cannot adapt to textual context. 

2.2. Cross-attention Based Fusion Module 

To overcome this limitation, a fusion module F (·) is introduced which conditions audio representations on textual prompts (See Fig-ure 1). Specifically, it builds upon a pretrained CLAP model and place a stack of cross-attention blocks on the audio encoder. Given an audio clip xa and a textual query prompt q, the pretrained audio and text encoders first produce token-level hidden representations 

Ha ∈ RTa×d, Hq ∈ RTq ×d, (3) where Ta and Tq are the numbers of audio and text tokens, respec-tively, and d is the hidden dimension. Each fusion block consists of a self-attention layer, a cross-attention layer, and a feed-forward network (FFN), each with a residual connection. Let H′ 

> a

denote the audio token representations after the self-attention layer. The cross-attention layer then uses H′

> a

as queries and the text representations Hq as keys and values: 

Hf = CrossAttn( Q = H′

> a

, K = Hq , V = Hq ), (4) where Hf denotes the fused audio-query token representations. Af-ter the subsequent FFN, the final token representations are aggre-gated using a pooling layer to produce a fixed-dimensional embed-ding ef , which serves as the query-conditioned audio representation for downstream training and inference. 

2.3. Audio-MCQ Supervision Framework 

Audio multiple-choice question (audio-MCQ) data is leveraged as a natural extension of the CLAP training paradigm. Given an audio clip xa, a question q, and M candidate answers {c1, . . . , c M }, the fused embedding is defined as discussed in Section 2.2: 

ef = F (Ha, Hq ), (5) and the text embeddings of candidate answers as 

em = Et(cm), m = 1 , . . . , M, (6) where e+ denotes the embedding of the correct answer. An InfoNCE loss for the fused embedding is adopted which aligns ef with e+ while contrasting it against answer choices from both the current and other questions in the mini-batch: 

Lf 

> InfoNCE

= − 1

N

> N

X

> i=1

log exp( s(eif , e+ 

> i

)/τ )

PNj=1 

PMm=1 exp( s(eif , ejm)/τ ) . (7) To further improve discrimination among candidates within each question, a cross-entropy loss is introduced: 

Lf 

> CE

= −

> M

X

> m=1

ym log exp( s(ef , em)) 

PMk=1 exp( s(ef , ek )) , (8) where ym is a one-hot label indicating the correct answer. Now, the overall audio-MCQ objective for the fused embedding is given as 

Lf 

> audio −MCQ

= Lf 

> InfoNCE

+ λLf

> CE

, (9) where λ is the weight of the cross-entropy loss. To maintain the general-purpose audio–text alignment learned during CLAP pretraining, two complementary regularisation terms are introduced. First, the standard CLAP contrastive loss LCLAP is retained by jointly training on audio–caption data. Second, the same audio-MCQ objective (Equation 9) is applied to the unfused audio embedding ea, where text embeddings are computed from concate-nated question–choice pairs: 

˜em = Et([ q; cm]) . (10) This yields an auxiliary loss Lu 

> audio −MCQ

for unfused em-bedding, defined analogously to Lf

> audio −MCQ

, but operating on 

(ea, ˜em) pairs. As a result, the final training objective is 

L = Lf 

> audio −MCQ

+ αLCLAP + βLu

> audio −MCQ

, (11) where α and β control the strengths of the regularisation terms. 

2.4. Attribute-Focused Audio-to-Audio Retrieval 

The audio-MCQ training stage equips TP-CLAP with the ability to inject textual prompts into audio representations. To adapt the model for attribute-focused audio-to-audio retrieval, it is further fine-tuned using audio classification data annotated with multiple attribute di-mensions, such as genre, mood, or instrumentation. For each at-tribute, a predefined question (e.g., “What is the genre of the mu-sic?”) is used as the text prompt, enabling the model to produce attribute-specific audio representations. To encourage the learned embedding to capture the target at-tribute, a classification objective is first applied. Let {l1, . . . , l M }

denote the candidate labels for the attribute, with corresponding text embeddings 

em = Et(lm). (12) The classification loss Lcls is defined similarly to Equation 8. In addition, an audio-to-audio contrastive objective is intro-duced. Rather than contrasting two query-conditioned embeddings, the loss is computed between the query-conditioned embedding ef

and the unfused audio embedding ea. This design allows each audio clip to be represented by a single embedding in the database dur-ing retrieval, avoiding the need to pre-compute and store separate embeddings for different attribute queries. For a mini-batch of N audio clips, an attribute similarity target 

tij ∈ [0 , 1] is defined between samples i and j. For single-label attributes, 

tij =

(

1, if the labels match ,

0, otherwise . (13) As for multi-label attributes, the target is computed using the intersection-over-union (IoU) between the corresponding label sets: 

tij = |Yi ∩ Yj ||Yi ∪ Yj | . (14) Then the audio-to-audio contrastive loss is given by 

La2a = − 1

N

> N

X

> i=1
> N

X

> j=1

tij log exp( s(eif , eju)/τ )

PNk=1 exp( s(eif , eku)/τ ) , (15) and the final fine-tuning objective is 

L = Lcls + γLa2a , (16) where γ controls the contribution of the contrastive objective. 

3. EXPERIMENTS 3.1. Implementation and Training Details 

TP-CLAP is built upon a CLAP backbone with CED-Base [11] and bert-base-uncased [12] as the audio and text encoders, respectively, each followed by a 1024-dimensional projection head. The proposed fusion module consists of L = 2 cross-attention blocks (hidden size 768, 12 attention heads) followed by another 1024-dimensional projection head. The base CLAP model is trained on AudioCaps-v2 [13], Clotho [14], WavCaps [15], MACS [16], LP-MusicCaps MC, and LP-MusicCaps MTT [17], totalling 601k audio–caption pairs. Training is performed for 20 epochs using the AdamW optimiser with a learning rate of 5 × 10 −5 and a 2-epoch warmup. TP-CLAP is further trained on the sound and music subsets of AudioMCQ [10] (310k question–choice pairs) in two stages: (1) op-timising only the fusion module for 5 epochs with a learning rate 

5×10 −5 and an 1-epoch warmup while freezing the CLAP encoders; and (2) jointly fine-tuning all parameters for 1 epoch on both audio– caption and AudioMCQ data using a learning rate of 5 × 10 −6. A batch size of 512 is used throughout, and audio longer than 10 sec-onds is randomly cropped during training. Hyperparameters τ , λ, α,

β, and γ are set to 0.07, 0.5, 1, 1, and 1, respectively. To isolate the effect of the fusion module, a baseline ( CLAP+ AudioMCQ ) is constructed which retains the original CLAP archi-tecture but is trained with the same data and hyperparameters as TP-CLAP using the objective of LCLAP + Lu 

> audio −MCQ

(Section 2). For attribute-focused audio-to-audio retrieval, TP-CLAP is fine-tuned on two music retrieval benchmarks: (1) NSynth [18], using the instrument family, pitch, and instrument source attributes, compris-ing 870k audio-attribute pairs; and (2) MagnaTagATune [19], using the genre, instrument, and tempo attributes, comprising 24k pairs. The models are trained for 30 epochs with a learning rate of 5×10 −5

and batch sizes of 512 and 256, respectively. 

3.2. Evaluation 

The models are first evaluated on standard audio–text retrieval tasks using the AudioCaps and Clotho test sets. Following prior work, retrieval performance is reported using Recall@1 (R@1). 

Table 1 . Audio–text retrieval results on AudioCaps and Clotho. Model AudioCaps Clotho T2A A2T T2A A2T MS-CLAP [3] 35.6 42.5 15.7 22.9 Laion-CLAP [5] 35.1 44.2 16.9 24.4 GLAP [6] 41.7 54.4 19.4 21.8 CLAP (ours) 42.1 55.9 21.0 26.6 CLAP+AudioMCQ 42.7 56.1 21.2 26.8 TP-CLAP (proposed) 42.6 57.2 21.3 27.1 

Zero-shot audio classification is then evaluated on eight bench-marks spanning sound (ESC50 [20], FSD50K [21], UrbanSound8K (US8K) [22], VocalSound (VS) [23], and CREMA-D (CD) [24]) and music (GTZAN [25], Beijing Opera (BO) [26], and NSynth in-strument) domains. Mean average precision (mAP) is reported for FSD50K, and accuracy is used for all other datasets. Note that, for TP-CLAP, retrieval and classification use the underlying CLAP en-coders rather than the fusion module. Next, audio understanding is evaluated on the sound and music subsets of MMAU [27] and MMAR [28]. For CLAP and CLAP+ AudioMCQ, answer scores are computed as the similarity be-tween the unconditioned audio embedding ea and the concatenated question–choice embedding ˜em. As for TP-CLAP, this score is in-terpolated with the similarity between the query-conditioned audio embedding ef and the choice embedding em using equal weights. Results are reported in terms of accuracy. Finally, attribute-focused audio-to-audio retrieval is evaluated on NSynth and MagnaTagATune. For NSynth, retrieval quality is measured using Precision@ K (P@ K), defined as the propor-tion of retrieved items sharing the target attribute with the query, and mAP. For MagnaTagATune, where attributes are multi-label, SoftPrecision@ K (SP@ K) and SoftmAP (SmAP) are used to ac-count for partial label overlap between retrieved items and queries. 

4. RESULTS 4.1. Audio–Text Retrieval 

Table 1 presents audio–text retrieval results on AudioCaps and Clotho. The proposed model is compared against representative CLAP-style baselines, including MS-CLAP [3], Laion-CLAP [5], and GLAP [6], which are trained on substantially larger and more diverse audio–caption corpora than ours. Despite using much less pretraining data, our CLAP model outperforms these baselines by leveraging the powerful CED audio encoder. Building on this, training with additional AudioMCQ data yields consistent improve-ments over the base CLAP model, suggesting that the fine-grained semantic supervision provided by the task benefits general audio– language alignment. The proposed TP-CLAP achieves the best overall performance, obtaining the highest Audio-to-Text R@1 score on AudioCaps and the best results for both metrics on Clotho. The improvements over CLAP+AudioMCQ indicate that the gains cannot be attributed solely to the inclusion of AudioMCQ data, but also to the proposed cross-attention-based fusion mechanism, which enables more effective token-level interaction between audio and textual information during training. 

4.2. Zero-Shot Audio Classification 

Table 2 reports zero-shot classification results on eight sound and music benchmarks. Our CLAP model establishes a stronger base-Table 2 . Zero-shot audio classification results (%). Numbers in grey are not obtained under a strict zero-shot setting. Model Sound Music Avg. ESC50 FSD50K US8K VS CD GTZAN BO NSynth MS-CLAP [4] 93.9 48.5 82.3 80.0 30.0 58.4 46.6 47.9 61.0 Laion-CLAP [5, 6] 91.0 21.5 77.0 79.3 18.3 47.4 40.2 26.1 50.1 GLAP [6] 88.8 40.9 78.9 75.1 20.5 69.6 36.5 31.3 55.2 CLAP (ours) 93.0 51.2 81.0 83.8 30.0 57.8 66.9 39.9 63.0 CLAP+AudioMCQ 93.0 52.1 81.9 84.1 33.1 56.9 65.3 40.6 63.4 TP-CLAP (proposed) 93.6 52.6 82.2 84.1 34.7 55.9 67.8 42.1 64.1 Table 3 . Audio question answering accuracy (%) of audio LLMs (top) and CLAP-based models (bottom) on MMAU and MMAR. Model Size MMAU MMAR Sound Music Sound Music Audio Flamingo 3 [29] 8B 79.58 73.95 53.33 50.97                                         

> Audio Flamingo 2 [30] 3B 71.47 70.96 24.85 17.48 Qwen2-Audio-IT [31] 7B 67.27 56.29 33.33 24.27 SALMONN [32] 13B 41.14 37.13 30.30 31.07 GAMA-IT [33] 7B 41.14 37.13 22.42 16.02 LTU [34] 7B 20.42 15.97 19.39 19.90 CLAP (ours) 199M 58.86 46.41 43.03 26.11 CLAP+AudioMCQ 199M 64.56 52.99 45.45 31.03 TP-CLAP (proposed) 219M 71.47 55.99 47.88 32.02

line than the selected CLAP variants reported in the literature. Com-pared to the base CLAP model, adding AudioMCQ data improves the average score from 63.0% to 63.4%, suggesting that the question-answering task enhances the discriminative power of the learned audio–language representations. TP-CLAP further increases the av-erage score to 64.1%, achieving the best performance on FSD50K, VocalSound, CREMA-D, Beijing Opera, and NSynth instrument. These improvements are consistent with the retrieval results in Ta-ble 1, indicating that both audio-MCQ supervision and the proposed fusion mechanism contribute to enhancing the transferability of the underlying CLAP representations. 

4.3. Audio Question Answering 

Table 3 compares TP-CLAP with representative audio-LLMs on MMAU and MMAR. These audio-LLMs combine pretrained audio encoders and LLMs and are instruction-tuned on diverse audio un-derstanding tasks, making them well suited for question answering. In contrast, TP-CLAP is built upon a lightweight CLAP architecture designed for contrastive representation learning and retrieval. Due to strong audio–language alignment, the base CLAP model already achieves competitive performance with several audio-LLMs, such as SALMONN [32] and GAMA-IT [33]. Incorporating Au-dioMCQ supervision (CLAP+AudioMCQ) further improves per-formance over the base model. The proposed fusion module then provides additional gains across all evaluation sets. These improve-ments stem from the cross-attention mechanism, which enables query-conditioned reweighting of audio features. Unlike CLAP+ AudioMCQ, which relies on fixed audio embeddings, TP-CLAP dynamically adapts audio representations to the input question, allowing more selective focus on task-relevant acoustic cues. As expected, Audio Flamingo 3 [29] achieves the strongest over-all performance owing to its substantially larger model capacity and scale of training data. Nevertheless, TP-CLAP performs compet-itively with other selected audio-LLMs, particularly on the sound subsets, despite using only 219M parameters. This highlights the effectiveness of contrastive representations for audio understanding without relying on an autoregressive language model. 

Table 4 . Attribute-focused retrieval results on NSynth. Attribute Setting P@5 P@10 P@20 mAP Instrument Unpr. 57.4 46.5 38.1 21.9 Pr. 79.0 78.4 77.0 44.2 

Pitch Unpr. 75.1 72.0 69.6 60.2 Pr. 87.9 86.7 84.2 72.3 

Source Unpr. 65.6 57.1 51.3 39.1 Pr. 78.8 78.8 78.8 58.2 Table 5 . Attribute-focused retrieval results on MagnaTagATune. Attribute Setting SP@5 SP@10 SP@20 SmAP Genre Unpr. 65.4 64.8 64.1 55.2 Pr. 66.3 66.4 66.3 58.0 

Instrument Unpr. 66.8 66.0 65.2 50.9 Pr. 69.7 70.0 69.5 54.9 

Tempo Unpr. 93.4 92.8 91.4 81.1 Pr. 96.5 96.5 96.6 90.2 4.4. Attribute-Focused Audio-to-Audio Retrieval 

Tables 4 and 5 present attribute-focused retrieval results on NSynth and MagnaTagATune. Conventional retrieval using the unprompted CLAP audio embedding is compared with retrieval using the query-conditioned representation produced by TP-CLAP. Prompted re-trieval outperforms the unprompted counterpart across all evaluated attributes and metrics, showing that TP-CLAP successfully adapts audio representations according to the requested criterion. Importantly, on both datasets, the unprompted retrieval per-formance consistently decreases from P@5 to P@20, whereas prompted retrieval remains comparatively stable across different retrieval depths. This suggests that the global CLAP embedding captures multiple acoustic factors simultaneously, causing retrieval rankings to become increasingly influenced by attributes unre-lated to the target criterion as more items are retrieved. In contrast, query-conditioned representations emphasise the requested attribute, resulting in a more coherent ranking of relevant samples. 

5. CONCLUSION 

This paper proposes TP-CLAP, a lightweight extension of CLAP that enables query-conditioned audio representations through a cross-attention fusion module. Using audio-MCQ supervision, TP-CLAP learns to incorporate textual prompts into audio embeddings while remaining compatible with the original CLAP framework. Ex-perimental results demonstrate that the proposed approach achieves competitive performance to several audio-LLMs on audio ques-tion answering despite its compact model size, while also improv-ing audio–text retrieval and zero-shot classification performance. Furthermore, TP-CLAP enabled attribute-focused audio-to-audio retrieval, allowing retrieval to be guided by user-specified character-istics rather than global semantic similarity. 6. REFERENCES 

[1] A. Radford, J.W. Kim, C. Hallacy, A. Ramesh, G. Goh, S. Agarwal, et al., “Learning transferable visual models from natural language supervision,” in Proc. ICML , Virtual, 2021. [2] T. Chen, S. Kornblith, M. Norouzi, and G. Hinton, “A simple framework for contrastive learning of visual representations,” in Proc. ICML , Virtual, 2020. [3] B. Elizalde, S. Deshmukh, M. Al Ismail, and H. Wang, “CLAP: Learning audio concepts from natural language supervision,” in Proc. ICASSP , Rhodes Island, Greece, 2023. [4] B. Elizalde, S. Deshmukh, and H. Wang, “Natural language su-pervision for general-purpose audio representations,” in Proc. ICASSP , Seoul, South Korea, 2024. [5] Y. Wu, K. Chen, T. Zhang, Y. Hui, T. Berg-Kirkpatrick, and S. Dubnov, “Large-scale contrastive language-audio pretrain-ing with feature fusion and keyword-to-caption augmentation,” in Proc. ICASSP , Rhodes Island, Greece, 2023. [6] H. Dinkel, Z. Yan, T. Wang, Y. Wang, X. Sun, Y. Niu, et al., “GLAP: General contrastive audio-text pretraining across do-mains and languages,” in Proc. ICASSP , Barcelona, Spain, 2026. [7] C. Xiao, H.P.K. Chan, H. Zhang, W. Xu, M. Aljunied, and Y. Rong, “Scaling language-centric omnimodal representation learning,” in Proc. NeurIPS , San Diego, CA, USA, 2025. [8] Y. Chen, S. Ji, H. Wang, Z. Wang, S. Chen, J. He, et al., “WavRAG: Audio-integrated retrieval augmented generation for spoken dialogue models,” in Proc. ACL , Vienna, Austria, 2025. [9] C. Tang, Q. Xiao, K. Mei, T. Wang, F. Rao, and C. Zhang, “WAVE: Learning unified & versatile audio-visual embeddings with multimodal LLM,” in Proc. ICLR , Rio de Janeiro, Brazil, 2026. [10] H. He, X. Du, R. Sun, Z. Dai, Y. Xiao, M. Yang, et al., “Mea-suring audio’s impact on correctness: Audio-contribution-aware post-training of large audio language models,” in Proc. ICLR , Rio de Janeiro, Brazil, 2026. [11] H. Dinkel, Y. Wang, Z. Yan, J. Zhang, and Y. Wang, “CED: Consistent ensemble distillation for audio tagging,” in Proc. ICASSP , Seoul, South Korea, 2024. [12] J. Devlin, M. Chang, K. Lee, and K. Toutanova, “BERT: Pre-training of deep bidirectional transformers for language un-derstanding,” in Proc. NAACL-HLT , Minneapolis, MN, USA, 2019. [13] C.D. Kim, B. Kim, H. Lee, and G. Kim, “AudioCaps: Gener-ating captions for audios in the wild,” in Proc. NAACL-HLT ,Minneapolis, MN, USA, 2019. [14] K. Drossos, S. Lipping, and T. Virtanen, “Clotho: An audio captioning dataset,” in Proc. ICASSP , Virtual, 2020. [15] X. Mei, C. Meng, H. Liu, Q. Kong, T. Ko, C. Zhao, et al., “WavCaps: A ChatGPT-assisted weakly-labelled audio captioning dataset for audio-language multimodal research,” 

IEEE/ACM TASLP , vol. 32, pp. 3339–3354, 2024. [16] I. Mart´ ın-Morat´ o and A. Mesaros, “What is the ground truth? Reliability of multi-annotator data for audio tagging,” in Proc. EUSIPCO , Dublin, Ireland, 2021. [17] S. Doh, K. Choi, J. Lee, and J. Nam, “LP-MusicCaps: LLM-based pseudo music captioning,” in Proc. ISMIR , Milan, Italy, 2023. [18] J. Engel, C. Resnick, A. Roberts, S. Dieleman, M. Norouzi, D. Eck, et al., “Neural audio synthesis of musical notes with WaveNet autoencoders,” in Proc. ICML , Sydney, NSW, Aus-tralia, 2017. [19] E.L. Law, L. Von Ahn, R.B. Dannenberg, and M. Crawford, “TagATune: A game for music and sound annotation.,” in Proc. ISMIR , Vienna, Austria, 2007. [20] K.J. Piczak, “ESC: Dataset for environmental sound classifi-cation,” in Proc. ACM Multimedia , Brisbane, QLD, Australia, 2015. [21] E. Fonseca, X. Favory, J. Pons, F. Font, and X. Serra, “FSD50K: An open dataset of human-labeled sound events,” 

IEEE/ACM TASLP , vol. 30, pp. 829–852, 2021. [22] J. Salamon, C. Jacoby, and J.P. Bello, “A dataset and taxonomy for urban sound research,” in Proc. ACM Multimedia , Orlando, FL, USA, 2014. [23] Y. Gong, J. Yu, and J. Glass, “VocalSound: A dataset for im-proving human vocal sounds recognition,” in Proc. ICASSP ,Singapore, 2022. [24] H. Cao, D.G. Cooper, M.K. Keutmann, R.C. Gur, A. Nenkova, and R. Verma, “CREMA-D: Crowd-sourced emotional multi-modal actors dataset,” IEEE TAC , vol. 5, no. 4, pp. 377–390, 2014. [25] G. Tzanetakis and P. Cook, “Musical genre classification of audio signals,” IEEE TSAP , vol. 10, no. 5, pp. 293–302, 2002. [26] M. Tian, A. Srinivasamurthy, M. Sandler, and X. Serra, “A study of instrument-wise onset detection in Beijing Opera per-cussion ensembles,” in Proc. ICASSP , Florence, Italy, 2014. [27] S. Sakshi, U. Tyagi, S. Kumar, A. Seth, R. Selvakumar, O. Ni-eto, et al., “MMAU: A massive multi-task audio understanding and reasoning benchmark,” in Proc. ICLR , Singapore, 2025. [28] Z. Ma, Y. Ma, Y. Zhu, C. Yang, Y. Chao, R. Xu, et al., “MMAR: A challenging benchmark for deep reasoning in speech, audio, music, and their mix,” in Proc. NeurIPS , San Diego, CA, USA, 2025. [29] S. Ghosh, A. Goel, J. Kim, S. Kumar, Z. Kong, S. Lee, et al., “Audio Flamingo 3: Advancing audio intelligence with fully open large audio language models,” in Proc. NeurIPS , San Diego, CA, USA, 2025. [30] S. Ghosh, Z. Kong, S. Kumar, S. Sakshi, J. Kim, W. Ping, et al., “Audio Flamingo 2: An audio-language model with long-audio understanding and expert reasoning abilities,” in Proc. ICML ,Vancouver, BC, Canada, 2025. [31] Y. Chu, J. Xu, Q. Yang, H. Wei, X. Wei, Z. Guo, et al., “Qwen2-Audio technical report,” arXiv preprint arXiv:2407.10759 , 2024. [32] C. Tang, W. Yu, G. Sun, X. Chen, T. Tan, W. Li, et al., “SALMONN: Towards generic hearing abilities for large lan-guage models,” in Proc. ICLR , Vienna, Austria, 2024. [33] S. Ghosh, S. Kumar, A. Seth, C.K.R. Evuru, U. Tyagi, S. Sak-shi, et al., “GAMA: A large audio-language model with ad-vanced audio understanding and complex reasoning abilities,” in Proc. EMNLP , Miami, FL, USA, 2024. [34] Y. Gong, H. Luo, A. Liu, L. Karlinsky, and J. Glass, “Listen, think, and understand,” in Proc. ICLR , Vienna, Austria, 2024.