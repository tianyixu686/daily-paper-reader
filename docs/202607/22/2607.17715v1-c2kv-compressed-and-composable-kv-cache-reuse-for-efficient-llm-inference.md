---
title: "C$^2$KV: Compressed and Composable KV Cache Reuse for Efficient LLM Inference"
title_zh: C$^2$KV：用于高效LLM推理的压缩与可组合KV缓存复用
authors: "Chuheng Du, Junyi Chen, Hanlin Tang, Kan Liu, Tao Lan, Lin Qu, Chaoyue Niu, Shengzhong Liu, Guihai Chen, Fan Wu"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.17715v1"
tags: ["query:mr"]
score: 9.0
evidence: 面向检索增强生成和多文档推理的KV缓存复用框架
tldr: 长上下文LLM推理中，KV缓存重用虽能减少预填充计算，但忽视了巨大的存储和访问开销。现有压缩与重用简单组合会导致精度下降。C$^2$KV学习位置无关的可组合压缩KV流形，通过轻量提取器与结构化注意力实现模块化重用。实验表明，在长上下文场景下实现高达17倍推理加速，同时保持生成质量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17715v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 828, \"height\": 394, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17715v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 862, \"height\": 506, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17715v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 828, \"height\": 318, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17715v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 847, \"height\": 554, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17715v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1572, \"height\": 508, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17715v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 749, \"height\": 549, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17715v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1734, \"height\": 865, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17715v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 868, \"height\": 452, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17715v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1549, \"height\": 407, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.17715v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 881, \"height\": 502, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17715v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 848, \"height\": 364, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17715v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 802, \"height\": 448, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17715v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1666, \"height\": 282, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17715v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1752, \"height\": 1386, \"label\": \"Table\"}]"
motivation: 现有KV缓存重用方法未解决存储瓶颈，且压缩与重用结合时精度严重下降。
method: 提出C$^2$KV框架，学习位置无关的压缩KV流形，用轻量提取器生成模块化表示，并通过压缩-拼接协同训练对齐。
result: 在长上下文基准测试中，KV缓存存储和传输成本显著降低，推理速度提升高达17倍，生成质量保持。
conclusion: C$^2$KV实现了高效且无损的非前缀KV重用，为长上下文LLM服务提供了可行方案。
---

## 摘要
长上下文推理是现代大型语言模型（LLM）应用（如检索增强生成和多文档推理）的核心。为了减轻日益增长的推理成本，最近的研究探索了键值（KV）缓存复用以减少冗余的预填充计算。然而，现有的复用方法主要关注计算节省，忽略了长上下文LLM服务中的一个关键瓶颈：存储和访问大型KV缓存的成本。虽然KV压缩似乎是自然的补充，但将压缩与非前缀KV复用简单结合通常会导致严重的精度下降。在这项工作中，我们提出了C$^2$KV，一个统一框架，用于非前缀KV复用，联合优化KV提取和推理时拼接。C$^2$KV学习了一个可组合且压缩的KV缓存流形，明确设计为位置无关。我们的方法引入了一个轻量级的侧车提取器，带有可学习的压缩令牌和结构化注意力流，实现了模块化的KV表示，可以灵活地复用和拼接，而无需修改冻结的基础模型。我们进一步采用压缩-拼接联合训练策略，将提取时的表示与其下游复用行为对齐。在多个长上下文基准和模型家族上的广泛实验表明，C$^2$KV显著降低了KV缓存存储和传输成本，在长上下文下实现了高达17倍的推理加速，同时保持了生成质量。

## Abstract
Long-context inference is central to modern large language model (LLM) applications such as retrieval-augmented generation and multi-document reasoning. To mitigate the growing inference cost, recent work has explored key-value (KV) cache reuse to reduce redundant prefill computation. However, existing reuse methods primarily focus on computation savings and overlook a critical bottleneck in long-context LLM serving: the cost of storing and accessing large KV caches. While KV compression appears to be a natural complement, naively combining compression with non-prefix KV reuse often leads to severe accuracy degradation. In this work, we propose C$^2$KV, a unified framework for non-prefix KV reuse that jointly optimizes KV extraction and inference-time concatenation. C$^2$KV learns a composable and compressed KV cache manifold that is explicitly designed to be position-agnostic. Our approach introduces a lightweight sidecar Extractor with learnable compression tokens and a structured attention flow, enabling modular KV representations that can be flexibly reused and concatenated without modifying the frozen base model. We further employ a compression-concatenation co-training strategy to align extraction-time representations with their downstream reuse behavior. Extensive experiments across multiple long-context benchmarks and model families demonstrate that C$^2$KV significantly reduces KV cache storage and transfer costs, achieving up to 17$\times$ inference speedup under long contexts, while preserving generation quality.

---

## 论文详细总结（自动生成）

# C²KV 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：长上下文LLM推理中，KV缓存重用虽能减少预填充计算，但现有方法（如CacheBlend、EPIC等）主要关注计算节省，忽略了两个关键瓶颈：(1) 存储和访问大型KV缓存的成本（内存带宽瓶颈）；(2) 现有压缩技术（如SnapKV）与非前缀KV重用简单结合时，因KV表示是上下文耦合且不可组合，导致严重精度下降。
- **研究动机**：实际部署中（如RAG、多文档推理），KV缓存重用面临存储和传输开销随上下文长度线性增长的问题，瓶颈已从计算转移到存储和内存带宽。因此需要统一设计压缩与复用，使KV表示既可压缩又可组合。
- **整体含义**：提出C²KV框架，学习位置无关（position-agnostic）的可组合压缩KV流形，通过轻量提取模块生成模块化表示，实现高效无损的非前缀KV重用，为长上下文LLM服务提供可行方案。

## 2. 方法论：核心思想、关键技术细节

### 2.1 核心思想
C²KV通过一个轻量级侧车模块（C2Extractor）附加在冻结的基础LLM上，将文档独立编码为压缩且可组合的KV缓存（C²KV）。这些C²KV表示是位置无关的，可在推理时灵活拼接和重新分配位置编码，无需任何blending或重计算。

### 2.2 关键技术细节

- **C2Extractor模块**：
  - 引入**C2Tokens**：作为可学习的压缩记忆槽，文档token序列按压缩比k:1分组，每组插入一个C2Token。所有C2Token共享同一可学习嵌入向量。
  - **新QKV投影头**：每层为C2Token设置单独的可训练QKV投影矩阵（维度与基础模型一致），原始token仍使用冻结的原始投影。参数增加约10%。
  - **残差均值池化变体（C²KV-Residual）**：在第一层，将每组k个文档token的隐藏状态均值加到对应C2Token的隐藏状态上，增强语义聚合。

- **结构化信息流（Structured Information Flow, SIF）**：
  - 注意力掩码设计确保：(1) 原始token只能注意其他原始token（标准因果掩码），严格禁止注意C2Token，因此原始token的隐藏状态与冻结模型一致；(2) 每个C2Token仅能注意自己块内的原始token和第一个块（sink块），以及因果地注意前面的C2Token；(3) 实现块局部（block-local）且因果累积的信息提取，防止全局信息泄露。

- **与锚点token压缩的对比**：锚点token使用双向注意力，会修改原始token表示并引入瓶颈，不适合重用；C²KV使用单向结构流，C2Token只作为KV提取器，原始token不变，因此生成可组合的KV表示。

- **压缩-拼接协同训练（Compression-Concatenation Co-Training）**：
  - 训练时，文档独立提取C²KV，然后拼接并与系统提示、查询一起输入基础模型生成答案，使用标准自回归语言建模损失（交叉熵）。
  - 梯度仅传播到C2Extractor参数，基础模型冻结。这种端到端监督迫使提取器学习在不同组合下都有效的KV表示。

- **推理时使用**：从存储中检索压缩的C²KV，根据目标位置重新计算旋转位置编码（RoPE），然后直接拼接，无需任何blending或重计算。

## 3. 实验设计

### 3.1 数据集与Benchmark
- **训练数据**：HotpotQA、2WikiMultiHopQA、LongMagpie（各采样40k，共120k样本），只训练1个epoch。
- **评估基准**：LongBench中的7个任务：HotpotQA、2WikiMQA、MuSiQue（多文档QA）、QMSum、MultiNews、GovReport（摘要）、SAMSum（对话摘要）。以及RULER（长上下文检索）和GSM8K（少样本数学）。

### 3.2 对比方法
- **全预填充（Full Recompute, FR）**：精度上限。
- **简单重用（Naive Reuse）**：直接拼接未校正的KV。
- **EPIC**：静态选择性重计算。
- **CacheBlend**：基于值差异的选择性重计算。
- **Block-Attention**：基于继续训练，修改注意力机制（仅用于Llama3.1-8B）。
- **SnapKV**：通用压缩方法（4倍压缩），与各种重用结合测试。

### 3.3 评估场景
- TTFT-精度权衡（7个任务×3个模型）
- 解码效率（时间每token vs. 上下文长度）
- 压缩比缩放（4x、8x、16x固定和动态训练）
- 长上下文RULER（4k-64k）
- 扩展到更大模型（Qwen3-14B）
- 消融研究（8个变体）

## 4. 资源与算力

- **训练配置**（附录B.1）：使用AdamW优化器，学习率5e-5，warmup 0.06，weight decay 0.1，全局batch size 32，梯度累积4，精度BF16。训练120k样本（1个epoch）。
- **未明确说明**：使用的GPU型号、数量、训练时长。论文未提供具体算力信息，可能影响复现。

## 5. 实验数量与充分性

- **实验数量**：共进行主体实验（TTFT/精度/解码效率）覆盖3个模型、7个任务、5个基线；额外实验包括压缩比缩放、动态训练、RULER、14B模型、消融8项；总量超过20组实验。
- **充分性与客观性**：
  - 实验较为充分，覆盖多种模型架构（Qwen和Llama系列）和参数规模（4B、7B、8B、14B）。
  - 指标客观：F1和ROUGE-L是标准评估，TTFT和TBT从系统层面衡量效率。
  - 消融研究系统性地验证了结构化信息流、位置重新对齐、压缩-拼接协同训练等关键设计。
  - 不足之处：主要聚焦于QA和摘要任务，未在更复杂的RAG流程（如多步推理、工具使用）或动态在线场景中评估；没有与最先进的KV压缩方法（如量化）进行联合对比。

## 6. 主要结论与发现

1. **KV缓存存储和内存带宽是长上下文推断的新的主要瓶颈**，现有重用方法未解决。
2. **朴素组合压缩与重用会导致严重精度损失**（表3验证）。
3. **C²KV在TTFT-精度权衡中占据最优区域**（图7），实现“load-only”操作，TTFT最低且精度接近全预填充。
4. **解码效率提升明显**：压缩KV后，时间每token随上下文增长保持平缓，128k上下文时显著优于其他方法。
5. **压缩比弹性好**：从4x到16x性能退化平缓，动态训练可推广到未见压缩比。
6. **扩展到更大模型（14B）依然有效**，C²KV-4x在部分任务上超过全预填充。
7. **消融验证了关键设计**：结构化信息流、位置重新对齐、局部块约束均不可或缺。

## 7. 优点（方法/实验设计亮点）

- **轻量级侧车设计**：不修改基础模型参数，易于迁移到新模型；训练开销低（仅训练C2Token和投影头，约10%参数）。
- **结构化信息流**：创新性注意力掩码确保原始token不变，同时支持信息聚合，避免锚点token方法的瓶颈问题。
- **压缩-拼接协同训练**：端到端训练直接对齐提取和重用行为，无需辅助损失，简洁有效。
- **位置重新对齐机制**：推理时根据拼接位置重新计算RoPE，保证位置感知。
- **实验全面性**：在多个模型大小、多个任务、多种压缩比、动态训练、长上下文场景下进行了系统评估，消融研究充分。
- **开源代码**：提供代码仓库，便于复现和扩展。

## 8. 不足与局限

- **仅支持文档级离线提取**：假设可重用内容可预先识别并离线编码，不支持在线或增量KV提取。
- **统一压缩比**：对所有文档和层使用相同压缩比，未考虑内容重要性和层敏感性，未来可探索自适应分配。
- **评估任务类型有限**：主要覆盖QA和摘要，缺乏对更复杂RAG流程（如多跳推理、代码生成、工具使用）的验证。
- **未与最新KV压缩方法（如量化、低秩分解）联合测试**：虽比较了SnapKV，但未测试与量化（如KVCacheQuant、MiniKV）的组合效果。
- **资源信息不透明**：未公开GPU型号、数量和训练时长，影响可复现性评估。
- **可能存在分布偏移风险**：训练数据仅来自三个数据集，可能无法泛化到其他领域或文档类型。
- **实用场景限制**：虽然TTFT和TBT有改善，但未深入分析真实端到端延迟（包括离线提取时间、存储成本等）的整体效益。

（完）
