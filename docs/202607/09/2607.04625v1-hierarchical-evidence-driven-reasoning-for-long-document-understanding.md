---
title: Hierarchical Evidence-Driven Reasoning for Long Document Understanding
title_zh: 分层证据驱动推理用于长文档理解
authors: "Junyu Xiong, Yonghui Wang, Rongjian Gu, Chenyu Liu, Bing Yin, Wengang Zhou, Houqiang Li"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.04625v1"
tags: ["query:mr"]
score: 9.0
evidence: 层次化多跳多模态RAG文档理解
tldr: "针对现有多模态RAG在长文档理解中检索易受主题相似但无答案的干扰页、单次流水线对初始检索依赖过强的问题，提出HIEVI-RAG框架。该框架通过层次化问题分解、粗粒度页面检索、基于GRPO训练的EVIAGENT进行细粒度跨页验证以及记忆引导的迭代生成四个阶段，实现证据驱动的层次化推理。在四个基准上平均准确率超越最强基线8.05%，展示了鲁棒性和协同性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04625v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1642, \"height\": 1074, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04625v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1660, \"height\": 402, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04625v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1635, \"height\": 581, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04625v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 840, \"height\": 216, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04625v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1662, \"height\": 499, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04625v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1662, \"height\": 279, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04625v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1657, \"height\": 307, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04625v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1658, \"height\": 483, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04625v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 826, \"height\": 343, \"label\": \"Table\"}]"
motivation: 现有RAG流水线面临检索干扰和单次依赖两大致命问题，导致遗漏核心证据引发级联错误。
method: 提出四阶段框架：层次化问题分解、粗粒度视觉检索、经GRPO训练的EVIAGENT细粒度跨页验证、记忆引导的迭代生成。
result: "在四个长文档理解基准上平均准确率领先现有开源方案8.05%，且各组件协同提升显著。"
conclusion: HIEVI-RAG通过证据驱动的层次化推理有效克服了传统RAG的检索和流水线缺陷。
---

## 摘要
检索增强生成（RAG）通过利用检索机制将输入图像限制为高度精选的子集，从而简化了长文档理解。然而，现有的多模态RAG流程主要面临两个关键挑战：首先，标准的语义相似性检索器经常获取主题重叠但缺乏答案的干扰页面，从而误导下游生成；其次，僵化的单次流程严重依赖初始检索的成功，任何核心证据的遗漏都必然导致级联错误。为应对这些挑战，我们引入了HIEVI-RAG，一种用于封闭域文档理解的分层、证据驱动的多模态RAG框架。HIEVI-RAG系统地将复杂查询分解为协作的四阶段流程：（1）分层问题分解，将多跳根查询分解为原子子问题；（2）粗粒度视觉页面检索，利用多模态检索器基于语义相似性获取候选页面；（3）通过EVIAGENT进行细粒度页面验证，EVIAGENT是一种使用GRPO训练的专业多页面验证器，可在多图像块上执行跨页面推理；（4）记忆引导的迭代生成，利用累积的子问题上下文对优先序列执行多轮动态推理。在四个基准上的广泛评估证明了我们框架的稳健有效性和协同作用，其显著优于现有开源基线，并在准确率上平均超过最强报告基线8.05%。

## Abstract
Retrieval-Augmented Generation (RAG) streamlines long-document understanding by leveraging retrieval mechanisms to restrict input images to a highly curated subset. However, existing multimodal RAG pipelines primarily face two critical challenges: first, standard semantic similarity retrievers frequently fetch topically overlapping yet answer-void distractor pages that mislead downstream generation; second, rigid single-pass pipelines heavily depend on initial retrieval success, where any omission of core evidence inevitably causes cascading errors. To address these challenges, we introduce HIEVI-RAG, a hierarchical, evidence-driven multimodal RAG framework for closed-domain document understanding. HIEVI-RAG systematically factorizes complex queries into a cooperative four-stage pipeline: (1) hierarchical question decomposition to break multi-hop root queries into atomic child questions; (2) coarse visual page retrieval leveraging a multimodal retriever to fetch candidate pages based on semantic similarity; (3) fine-grained page verification via EVIAGENT, a specialized multi-page verifier trained with GRPO to execute cross-page reasoning over multi-image blocks; and (4) memory-guided iterative generation that leverages accumulated sub-question context to execute multi-round, dynamic reasoning over the prioritized sequence. Extensive evaluations across four benchmarks demonstrate the robust efficacy and synergy of our framework, which significantly outperforms existing open-source baselines and exceeds the strongest reported baseline by an average of 8.05% in accuracy.

---

## 论文详细总结（自动生成）

# 论文详细中文总结：HIEVI-RAG

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：现有闭域长文档理解中的多模态RAG管道面临两大致命缺陷：
  1. **检索干扰**：基于语义相似度的检索器经常召回主题相关但实际不含答案的干扰页面，误导下游生成模型。
  2. **单次依赖性**：僵化的单次（single-pass）流程完全依赖于初始检索的成功；若核心证据被遗漏，后续生成必然发生级联错误，无法修正。
- **背景**：在科学、金融、企业知识管理等场景中，用户需从数百页包含文本、表格、图表的文档中提取稀疏分布的答案。全量输入超出LLM上下文窗口，RAG成为主要方案，但现有方法受限于上述两大瓶颈。
- **整体含义**：论文提出HIEVI-RAG，通过分层证据驱动的多阶段推理，系统性地解决检索精读不足和级联错误问题，显著提升长文档问答准确率。

## 2. 方法论：核心思想、关键技术细节
- **核心思想**：将复杂查询分解为浅层推理层次，通过“粗检→细验→迭代生成”的协同流程，实现证据的精确提取与推理。
- **四阶段流程**：
  1. **分层问题分解**：使用轻量级LLM（Qwen3.5-4B）将根查询分解为最多5个原子子问题，降低单次检索难度，保证分散证据的召回。
  2. **粗粒度视觉页面检索**：基于ColPali风格的Ops-ColQwen3-4B多模态检索器，在线编码查询（根查询+子问题），与离线构建的页面多向量索引进行延迟交互匹配，为每个查询返回Top-20候选页面。
  3. **细粒度页面验证（EVIAGENT）**：
     - 使用GRPO（Group Relative Policy Optimization）对基座VLM（Qwen3-VL-8B-Instruct）进行训练，专门执行跨页推理和证据判断。
     - 训练时，模型输出结构包含：`<think>`推理轨迹、`<evidence_page>`二进制证据序列（T/F，与输入图像顺序对齐）、`<answer>`最终答案。
     - 奖励函数为加权和：\( R = \lambda_{\text{fmt}} r_{\text{fmt}} + \lambda_{\text{evi}} r_{\text{evi}} + \lambda_{\text{acc}} r_{\text{acc}} \)，其中证据奖励使用F1分数，权重（0.1, 0.5, 0.4）。
     - 推理时，将每个候选列表按块大小k=7分割，对每个块调用EVIAGENT进行联合验证。对于子问题，仅保留验证为证据的页面（其余丢弃）；对于根问题，按“证据页优先→非证据页按原排序在后”构造优先序列。
  4. **记忆引导的迭代生成**：
     - **初始化记忆**：先逐个回答每个子问题（使用其验证后的证据页），将子问题、推理轨迹、中间答案累积到文本记忆中。
     - **迭代推理**：对根问题，以滑动窗口（k=7）扫描优先序列，每个窗口输入根查询、当前记忆、当前图像块。若模型输出`NOT_ANSWERABLE`，则将当前推理轨迹追加到记忆，取下一个窗口继续；否则返回答案作为最终结果。
- **公式/算法**：无复杂数学公式，主要通过上述文字描述的流程实现。

## 3. 实验设计
- **数据集（Benchmark）**：四个公开长文档QA基准，统计如下：
  - PaperTab：393样本，2~152页，平均10.7页（科学表格）。
  - FetaTab：1016样本，2~216页，平均16.3页（维基百科风格表格）。
  - MMLongBench：1082样本，9~468页，平均47.8页（长多模态文档）。
  - LongDocURL：2325样本，51~149页，平均89.0页（综合长文档理解）。
- **对比方法**：
  - M3DocRAG (7B)
  - MDocAgent (7B)
  - MoLoRAG+ (7B)
  - ALDEN (7B)
  - URaG (7B)
  - Doc-V* (7B，GRPO优化模型)
  - 以及自身消融变体。
- **评估指标**：
  - 主指标：准确率（Accuracy）。
  - 检索指标：Recall@K、NDCG@K、MRR@K。

## 4. 资源与算力
- **未明确说明训练时长**，但指出：
  - EVIAGENT基于Qwen3-VL-8B-Instruct，使用GRPO训练，在**8×NVIDIA A100 GPU集群**上执行。
  - 分解使用Qwen3.5-4B，检索使用Ops-ColQwen3-4B（4B参数）。
- **计算开销**：论文在“局限性”中承认框架推理阶段涉及多次LLM/VLM调用（分解、多块验证、迭代生成），导致累积延迟和成本高于单次流水线，但未给出具体FLOPs或时间数据。

## 5. 实验数量与充分性
- **实验组数**：
  - **主实验**：在4个数据集上与6种最新方法对比，报告准确率（表2）。
  - **消融实验**：对三个核心模块（证据验证、层次分解、迭代推理）分别消融（表3），覆盖全部4个数据集，共12个消融比较。
  - **骨干/检索器敏感性分析**：交叉组合2种生成器（Qwen2.5-VL-7B、Qwen3-VL-8B）与2种检索器（ColQwen2.5、Ops-ColQwen3-4B），共5组配置对比（表4）。
  - **检索性能分析**：在MMLongBench和LongDocURL上，比较不同Top-K（1,3,5）下基线、未训练VLM、EVIAGENT的Recall/NDCG/MRR（表5，18个指标对比）。
  - **块大小敏感性**：在MMLongBench上测试k=3,5,7,9,11时的NDCG@1和Recall@5（图2）。
- **充分性评价**：
  - 实验覆盖4个不同规模与类型的文档QA数据集，包含表格、科学、长文档等场景。
  - 消融实验全面验证了每个组件的贡献，且所有消融都造成一致性下降，证明协同性。
  - 与当前最强开源基线对比，并做了骨干/检索器交叉验证。实验设计客观、公平（控制变量，相同骨干下仍优于基线）。
  - **唯一不足**：未在真实生产环境或跨域（如手写体、低质量扫描）测试，但论文在局限性中明确承认。

## 6. 主要结论与发现
- HIEVI-RAG在全部四个基准上**显著超越所有开源基线**，平均准确率提升8.05%（相对于最强基线）。
- 相比MoLoRAG+（7B），在PaperTab上提升13.0个百分点；相比Doc-V*（7B），在LongDocURL上提升9.4个百分点。
- 消融实验表明：移除证据验证导致平均-4.5%绝对下降，移除层次分解导致-2.7%，移除迭代推理导致-1.9%，验证了三者缺一不可。
- EVIAGENT经过GRPO训练后，检索NDCG@1在MMLongBench上从70.4%提升至78.3%，且未训练的VLM无法带来收益（甚至降低），说明专用训练的必要性。
- 最优块大小k=7，过小限制跨页推理，过大致视觉上下文过载。

## 7. 优点（方法与实验设计的亮点）
- **方法层面**：
  - 将“验证”从单页独立判断提升为**跨页联合推理**，有效过滤主题相似但无答案的干扰页。
  - 记忆引导的迭代生成机制允许**动态回溯与累积上下文**，克服单次检索的级联错误。
  - 证据奖励使用F1分数而非简单匹配，防范奖励作弊，提升验证真实性。
- **实验层面**：
  - 在相同骨干（Qwen2.5-VL-7B + ColQwen2.5）下，HIEVI-RAG仍优于MoLoRAG+，证明改进来源于架构而非模型规模。
  - 对每个模块进行了严格的消融，且结果一致（移除任何模块均下降），强有力地证明了协同效应。
  - 检索性能分析区分了“训练前后EVIAGENT”的差异，清晰说明纯推理VLMs无法胜任验证任务。

## 8. 不足与局限
- **计算开销**：多阶段（分解、多块验证、迭代推理）导致推理延迟和成本显著高于单次流水线，不适合实时性要求高的场景。
- **对中间推理质量的依赖**：记忆引导生成依赖子问题推理轨迹的准确性；若子问题产生严重幻觉或偏差，可能污染记忆并误导最终答案。
- **应用域限制**：优化和评估主要基于电子文档（born-digital），对**手写体、低质量扫描、自然场景文字**的零样本泛化能力未经验证。
- **可扩展性**：虽然框架可处理上百页文档，但若文档页数极大（如数千页），粗检索的Top-K固定为20可能漏掉证据，需调整K值或增加自适应策略。
- **未提及公平性**：论文未讨论数据偏差或模型在少数群体文档上的表现。

（完）
