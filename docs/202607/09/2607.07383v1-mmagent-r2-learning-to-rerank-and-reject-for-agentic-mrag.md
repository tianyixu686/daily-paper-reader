---
title: "MMAgent-R$^2$: Learning to Rerank and Reject for Agentic mRAG"
title_zh: MMAgent-R$^2$：学习重排序与拒绝以改进智能体式mRAG
authors: "Tao Zhang, Ziqi Zhang, Zongyang Ma, Yuxin Yang, Bing Li, Chunfeng Yuan, Kang Rong, Fengyun Rao, Jing Lyu, Weiming Hu"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07383v1"
tags: ["query:mr"]
score: 9.0
evidence: 用于知识型VQA的智能体多模态RAG，包含重排和拒绝机制
tldr: 知识型视觉问答中，现有多模态检索增强方法因依赖全局特征，难以区分视觉相似实体，错误会传播至答案。本文提出MMAgent-R^2，通过视觉重排序比较图像细节，并引入主动拒绝以动态补充候选集，结合复合奖励函数进行GRPO训练。在InfoSeek、E-VQA和MMhops上达到最优，尤其在困难检索和多图多跳推理上优势显著。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07383v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1257, \"height\": 858, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07383v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1266, \"height\": 730, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07383v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1261, \"height\": 760, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07383v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1257, \"height\": 649, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.07383v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1257, \"height\": 401, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07383v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1261, \"height\": 844, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07383v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 611, \"height\": 300, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07383v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 644, \"height\": 278, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07383v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 666, \"height\": 179, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07383v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 512, \"height\": 180, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07383v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 583, \"height\": 210, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07383v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 538, \"height\": 180, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07383v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 956, \"height\": 344, \"label\": \"Table\"}]"
motivation: 现有mRAG在视觉相似实体多时检索失败，且错误无法在固定候选集中纠正。
method: 提出MMAgent-R^2框架，集成视觉重排序和主动拒绝，并用GRPO联合优化外部检索、内部验证和答案生成。
result: 在InfoSeek、E-VQA、MMhops三个基准上取得SOTA性能。
conclusion: 通过主动拒绝和视觉重排序实现了更精准的检索与验证，缓解了错误传播问题。
---

## 摘要
基于知识的视觉问答（KB-VQA）要求模型从大规模百科知识库中检索与查询图像匹配的视觉实体，并回答相关问题。现有多模态检索增强生成（mRAG）方法依赖全局视觉特征来匹配候选实体，然而当知识库包含大量视觉相似的实体时，检索器难以区分它们，从而在候选集中填充了视觉相似但事实不匹配的干扰项。由于后续处理步骤（如噪声过滤）也被限制在这个固定的候选集内，检索失败的错误不可避免地传播到最终答案。为了解决这些挑战，我们提出MMAgent-R$^2$，一种智能体式mRAG框架，该框架将视觉重排序和主动拒绝作为其内部验证机制。视觉重排序直接比较查询图像和候选图像，捕获文本描述之外的区分性细节，以在相似候选者中精确识别目标实体；主动拒绝则丢弃不可靠的结果，并在未找到可靠匹配时检索额外的候选者，从而超越了固定的候选池。我们设计了一个复合奖励函数，包含步骤级验证奖励，并通过GRPO训练实现了外部检索、内部验证和答案生成的联合优化。在InfoSeek、E-VQA和MMhops上的实验表明，我们的方法达到了最先进的性能，在具有挑战性的检索场景和复杂的多图像多跳推理任务中尤其具有显著优势。

## Abstract
Knowledge-based Visual Question Answering (KB-VQA) requires models to retrieve visual entities matching the query image from large-scale encyclopedic knowledge bases and answer related questions. Existing multimodal Retrieval Augmented Generation (mRAG) methods rely on global visual features to match candidate entities, yet when the knowledge base contains numerous visually similar entities, the retriever struggles to distinguish them, populating the candidate set with visually similar but factually mismatched distractors. Since subsequent processing steps such as noise filtering are also confined to this fixed candidate set, errors from failed retrieval inevitably propagate to the final answer. To address these challenges, we propose MMAgent-R$^2$, an agentic mRAG framework that integrates visual reranking and active rejection as its internal verification mechanism. Visual reranking directly compares query and candidate images, capturing discriminative details beyond textual descriptions to precisely identify the target entity among similar candidates; active rejection discards unreliable results and retrieves additional candidates when no confident match is found, moving beyond the fixed candidate pool. We design a composite reward function with step-level verification rewards and achieve joint optimization of external retrieval, internal verification, and answer generation via GRPO training. Experiments on InfoSeek, E-VQA, and MMhops demonstrate that \ours{} achieves state-of-the-art performance, with particularly notable advantages in challenging retrieval scenarios and complex multi-image multi-hop reasoning tasks.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

- **研究动机**：基于知识的视觉问答（KB-VQA）需要从大规模百科知识库中检索与查询图像匹配的视觉实体并回答问题。现有多模态检索增强生成（mRAG）方法依赖全局视觉特征（如CLIP）进行图像级检索，但当知识库中存在大量视觉相似的实体时，检索器难以区分，候选集中充斥视觉相似但事实不匹配的干扰项。
- **核心问题**：后续处理步骤（如噪声过滤）被限制在固定候选集中，一旦正确实体未被召回，错误会传播至最终答案。此外，传统pipeline依赖额外模块或固定步骤，难以联合优化，且无法有效处理复杂的多图多跳推理。
- **整体含义**：本文旨在**打破固定候选集限制**，引入**视觉级别的内部验证机制**，使模型在检索后能主动识别目标实体或拒绝不可靠结果，从而显著提升KB-VQA在困难检索场景下的鲁棒性和准确性。

## 2. 提出的方法论

- **核心思想**：将KB-VQA建模为**智能体多轮决策过程**，定义动作空间包含“外部检索”、“内部验证”、“答案生成”。通过强化学习（GRPO）联合优化外部检索、内部验证和答案生成。
- **关键技术细节**：
  - **动作空间设计**（表1）：
    - **外部检索**：
      - `SearchImage(j)`：基于查询图像索引j，从视觉知识库中返回Top-K候选批次；支持连续调用返回非重叠的后续批次（动态扩展）。
      - `SearchText({q_i})`：并行提交多个文本查询，返回相关段落。
    - **内部验证**：
      - `Rerank(ek)`：直接比较查询图像与候选图像，通过视觉细节识别匹配的实体并输出其名称。
      - `Reject()`：当当前批次无匹配时，主动拒绝并触发下一批次的检索（允许最多Rej_max次拒绝）。
    - **答案生成**：`Answer(y)`，基于累积证据生成最终答案。
  - **智能体推理循环**：每轮执行“推理→动作→环境反馈”，支持灵活切换检索、验证、拒绝动作，直到答案生成或达到最大轮次T_max。
  - **训练方法**：采用 **GRPO**（Group Relative Policy Optimization），每样本采样G=8条完整rollout轨迹。
    - **复合奖励函数**（式2-4）：
      - `R_outcome`：最终答案是否正确（0/1）。
      - `R_format`：确保动作格式合规。
      - `R_ver`：**步骤级验证奖励**，对每个验证步骤（Rerank/Reject）给予细粒度监督：
        - 若正确拒绝（实体不在当前批次），奖励δ（默认0.2）；
        - 若正确选择目标实体（Rerank(e*)），奖励1；
        - 否则0。
    - **环境响应遮蔽**：只对模型自身生成的推理和动作token计算梯度，环境反馈（检索结果等）不参与更新。

- **公式与算法**：无复杂公式，主要定义奖励函数。算法流程见附录算法S1。

## 3. 实验设计

- **数据集与场景**：
  - **InfoSeek**：约1.3M图像-问题-答案对，覆盖11K Wikipedia实体，包含Unseen Question和Unseen Entity子集。知识库100K页面。
  - **Encyclopedic-VQA（E-VQA）**：221K问答对，最高5张图像/样本，覆盖16.7K细粒度类别，包含单跳和两跳推理。知识库2M页面，检索难度高。
  - **MMhops**：31.1K样本，设计为多跳推理，包含Bridging（单图像链式）和Comparison（多图像对比）任务。知识库100K页面。
- **评估指标**：
  - InfoSeek：字符串和时间类型用VQA准确率（精确匹配，时间±1年容忍），数值用Relaxed Accuracy（10%容忍或IoU≥50%）；总体为调和平均。
  - E-VQA：使用BEM（BERT Matching）得分≥0.5判断正确。
  - MMhops：同InfoSeek评估协议。
- **对比方法**：
  - **直接回答**：InstructBLIP, BLIP-2, GPT-4V, Qwen2.5-VL-7B, Qwen3-VL-8B等。
  - **多模态RAG**：DPR v+t, RORA-VLM, CoMEM, Wiki-LLaVA, mKG-RAG, EchoSight, mR²AG, MMKB-RAG, VLM-PRF, QKVQA, ReAG等。
  - **智能体mRAG**：MMhops-R1（同样为智能体方法但无视觉验证）。
  - **闭源模型**：Gemini-2.5-flash/pro（仅在MMhops上）。
- **消融实验**（在InfoSeek子集上）：
  - 移除重排序和拒绝、仅重排序、完整方法。
  - 候选批次大小K（3,5,7）。
  - 最大拒绝次数（0,1,2,3）。
  - 拒绝奖励权重δ（0.1,0.2,0.3）。

## 4. 资源与算力

- **文中说明**：
  - 基座模型：Qwen2.5-VL-7B-Instruct（~7B参数）和Qwen3-VL-8B-Instruct（~8B参数）。
  - 训练框架：verl（基于GRPO），全局batch size=1024，训练2个epoch，每样本G=8个rollout。
  - 学习率1e-6，最大推理轮次T_max=8。
  - 检索器：EVA-CLIP-8B编码图像和标题，Faiss-GPU索引；文本检索用E5，段落800字符分块。
- **未明确说明**：文中未明确说明使用的**GPU型号、数量、训练时长**。仅提到Faiss-GPU、FastAPI等，但未提供算力配置细节。

## 5. 实验数量与充分性

- **实验数量**：
  - 主实验：3个数据集（InfoSeek, E-VQA, MMhops）总计多个子集（Unseen-Q/Unseen-E, Single-Hop/All, Bridging/Comparison），报告完整结果。
  - 消融实验：4组（表5-8），分别考察重排序/拒绝影响、批次大小K、最大拒绝次数、拒绝奖励权重。
  - 定性分析：图3、图4展示多组具体案例。
- **充分性与公平性**：
  - 对比方法涵盖三类baseline（直接回答、多模态RAG、智能体mRAG），且在同一或相近知识库设置下比较。
  - 消融实验控制变量，结果清晰显示各组件的贡献。
  - 但在训练数据上，仅从各数据集取子集（65K/67K/17K），未使用全部训练数据，可能对最终性能有影响。不过对比方法也多在相似子集上训练（如ReAG等）。
  - 未报告标准差或多次运行结果，单次结果可能受随机性影响。
- **总体评价**：实验设计全面，消融充分，对比对象合理，但算力资源和随机性细节缺失。

## 6. 主要结论与发现

- MMAgent-R²在三个基准上均达到**最先进（SOTA）性能**，尤其：
  - **E-VQA**：整体准确率54.2%（+7.2 over ReAG），因该知识库规模大（2M页）、检索难度最高（Recall@1仅15.4%），视觉重排序和主动拒绝带来的增益最显著。
  - **MMhops**：Bridging提升13.2%，Comparison提升10.4%，验证了方法在多图多跳推理中的有效性。
- 实体识别准确率显著提升：在E-VQA上从15.4%（Recall@1）提升至29.5%（+14.1），在InfoSeek上从53.7%提升至63.6%（+9.9）。
- 消融实验表明：**视觉重排序是主要增益来源**（+4.1% VQA准确率），主动拒绝进一步提供额外提升（+1.2%），两者互补。
- 候选批次大小K=5、最大拒绝次数2、拒绝奖励权重δ=0.2为最佳配置，平衡了效率与性能。

## 7. 优点

- **创新性**：首次将**视觉级别的重排序**和**主动拒绝**引入智能体mRAG，突破文本级后处理的局限，直接利用视觉细节区分相似实体。
- **端到端训练**：通过GRPO和复合奖励函数（含步骤级验证奖励）联合优化检索、验证、生成，无需额外模块或手工规则。
- **灵活性**：智能体动作空间支持动态扩展候选集，适应不同检索难度；多图多跳自然兼容。
- **实验充分**：在难度各异的数据集上均取得显著提升，消融实验验证了各组件必要性，且有定性案例分析。

## 8. 不足与局限

- **仍受检索器召回上限限制**：如InfoSeek上，实体识别准确率63.6%仍低于可达到的理论上限（R@15为80.2%），说明即使有验证，初始召回仍存在瓶颈。
- **token开销**：注入大量候选图像（特别是多次拒绝后）会增加推理成本，可能影响效率。
- **未与检索器联合优化**：当前检索器是固定的（EVA-CLIP-8B），若能与智能体一起训练可能进一步提升。
- **实验覆盖有限**：仅测试了三个KB-VQA数据集，未在更多样化的场景（如开放域VQA、多模态问答）中验证泛化性。且仅在7B/8B模型上验证，未探索更大模型。
- **算力细节缺失**：未提供训练的具体硬件配置和时间，不利于复现。
- **潜在偏差**：训练样本过滤了那些目标实体落在可检索范围内的样本，可能导致模型对不可达案例处理能力不足，但评估时包括了所有样本，因此结果更可信。

（完）
