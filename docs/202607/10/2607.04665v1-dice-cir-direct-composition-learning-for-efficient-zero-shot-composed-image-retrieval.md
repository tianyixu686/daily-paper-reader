---
title: "DiCE-CIR: Direct Composition Learning for Efficient Zero-Shot Composed Image Retrieval"
title_zh: "DiCE-CIR: 面向高效零样本组合图像检索的直接组合学习"
authors: "Gwang-Ho Na, Ho-Joong Kim, Seong-Whan Lee"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.04665v1"
tags: ["query:mr"]
score: 9.0
evidence: 通过直接组合学习的零样本组合图像检索
tldr: 零样本组合图像检索中，现有投影方法需要额外步骤导致效率低且训练推理不一致。本文提出DiCE-CIR，直接组合参考图像和编辑文本，无需投影。利用大语言模型从图像-文本对自动构造训练样本，训练轻量组合模块。在CIRCO上取得最优，CIRR上竞争力强，且计算高效。避免了投影方法的缺陷，实现了高效直接的零样本组合检索。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04665v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 877, \"height\": 818, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04665v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1722, \"height\": 820, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04665v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 888, \"height\": 901, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04665v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1613, \"height\": 616, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04665v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 850, \"height\": 468, \"label\": \"Table\"}]"
motivation: 现有投影方法存在训练推理不一致、计算效率低的问题，需要更直接的组合学习方案。
method: 提出DiCE-CIR，直接组合图像与文本，用大语言模型自动构建训练样本，训练轻量组合模块实现高效对齐。
result: 在CIRCO上达到SOTA，CIRR上性能有竞争力，且计算开销显著低于投影方法。
conclusion: 提出直接组合学习框架，无需投影步骤，实现了高效且准确的零样本组合图像检索。
---

## 摘要
零样本组合图像检索（ZS-CIR）旨在根据由参考图像和描述所需修改的编辑文本组成的多模态查询，检索目标图像。最近的ZS-CIR研究依赖于基于投影的方法，将参考图像映射到文本嵌入空间中的伪词标记。然而，此类方法需要额外的投影和重新编码步骤，增加了训练复杂度，降低了效率，并在训练与推理之间引入了差异。本文提出DiCE-CIR，一种直接组合学习方法，通过直接组合参考图像和编辑文本预测组合查询表示。为实现无需手动标注三元组的可扩展训练，我们利用大规模语言模型从大规模图像-文本对自动构建组合训练样本。基于这些样本，我们训练一个轻量级组合模块，其目标函数促进与目标的对齐、编辑一致的语义变换以及检索判别性。我们在ZS-CIR基准上进行了广泛实验，结果表明DiCE-CIR在CIRCO上达到最先进性能，在CIRR上具有竞争力，同时保持高计算效率。

## Abstract
Zero-shot composed image retrieval (ZS-CIR) aims to retrieve a target image from a multimodal query consisting of a reference image and an edit text describing the desired modification. Recent ZS-CIR studies have relied on projection-based methods that map a reference image into pseudo-word tokens in the text embedding space. However, such methods require additional projection and re-encoding steps, increasing training complexity, reducing efficiency, and introducing a discrepancy between training and inference. In this paper, we propose DiCE-CIR, a direct composition learning method that predicts composed query representations by directly composing a reference image and an edit text. To enable scalable training without manually annotated triplets, we automatically construct compositional training samples from large-scale image-caption pairs using a large language model. Based on these samples, we train a lightweight composition module with objectives that promote alignment with the target, edit-consistent semantic transformation, and retrieval discriminability. We conduct extensive experiments on ZS-CIR benchmarks and show that DiCE-CIR achieves state-of-the-art performance on CIRCO and competitive performance on CIRR while maintaining high computational efficiency.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究任务**：零样本组合图像检索（Zero-shot Composed Image Retrieval, ZS-CIR），即根据一张参考图像和一段编辑文本组成的多模态查询，从数据库中检索目标图像。
- **现有方法局限**：主流基于投影的方法（如Pic2Word、SEARLE、LinCIR）需将参考图像映射为伪词标记，再与编辑文本拼接后重新编码，导致两个问题：
  - **训练顺序依赖**：投影和重新编码步骤串联，增加训练复杂度与计算开销。
  - **训练-推理不一致**：训练时优化的是映射到伪词，而非直接优化用于检索的组合查询，造成目标偏差。
- **本文动机**：提出直接组合参考图像和编辑文本的方法，避免中间投影和重新编码，提升效率并消除不一致性。

### 2. 论文提出的方法论

- **核心思想**：直接组合参考图像和编辑文本的嵌入，预测组合查询嵌入，无需将图像转为伪词。
- **关键技术细节**：
  - **自动数据构建**：利用大语言模型（LLM，Qwen3.5-9B）从CC3M图像-文本对生成编辑文本和目标描述。每张图像的原描述作为参考描述，LLM根据5种编辑类型（细粒度属性、空间/视角、强场景变化、组合双变化、简短检索）生成编辑文本和目标描述，得到约122万训练样本（无需人工三元组）。
  - **组合模块Φ**：轻量级门控MLP，输入为参考嵌入（图像或文本）与编辑文本嵌入的拼接，经层归一化后通过两个并行线性投影（一个带GELU激活），再点积后线性投影得到组合查询嵌入。
  - **训练目标**（三个损失）：
    - **对齐损失**（\( \mathcal{L}_{\text{align}} \)）：最小化组合查询嵌入与目标描述嵌入的余弦距离。
    - **残差对齐损失**（\( \mathcal{L}_{\text{delta}} \)）：对齐从参考图像到组合查询的残差与从参考描述到目标描述的残差方向，确保编辑语义一致。
    - **对比损失**（\( \mathcal{L}_{\text{ctr}} \)）：批内对比学习，拉近匹配对、推开非匹配对。
  - **总损失**：\( \mathcal{L} = \lambda_{\text{align}} \mathcal{L}_{\text{align}} + \lambda_{\text{delta}} \mathcal{L}_{\text{delta}} + \lambda_{\text{ctr}} \mathcal{L}_{\text{ctr}} \)，权重分别为1.0、1.0、0.2。
- **推理**：直接使用训练好的Φ组合参考图像嵌入和编辑文本嵌入，与候选图像嵌入计算余弦相似度排序。

### 3. 实验设计

- **数据集**：
  - 训练数据：从CC3M（约330万张图像）自动构建122万组成对样本。
  - 测试基准：**CIRCO**（开放检索，多有效目标）和**CIRR**（标准检索，单目标）。评估指标：CIRCO用mAP@K（K=5,10,25,50）；CIRR用R@K和Rs@K。
- **对比方法**：投影类方法（Pic2Word、SEARLE、FTI4CIR、LinCIR）、生成类方法（CIG、PrediCIR）以及简单的图像/文本/图像+文本基线。
- **公平性**：使用相同CLIP骨干（ViT-L/14和ViT-G/14），在相同环境下复现部分方法（标记†），报告时区分不同训练数据量（如Pic2Word用3M图像，LinCIR用5.5M文本等）。

### 4. 资源与算力

- **训练设备**：2张 NVIDIA RTX A6000 GPU。
- **训练时长**：ViT-L/14骨干约0.2小时，ViT-G/14约0.3小时。
- **推理时间**：ViT-L/14为0.01秒，ViT-G/14为0.02秒。
- **参数**：门控MLP仅1181万参数（ViT-L），显著少于三层MLP的1653万。
- 文中明确说明了GPU型号、数量和训练时间，算力公开透明。

### 5. 实验数量与充分性

- **主要实验**：主表（Table I）比较了8种方法在CIRCO和CIRR上的性能，包含两个骨干。
- **消融实验**（Table II）：
  - A组：移除三个损失各自的影响（3组）。
  - B组：架构和训练策略（三层MLP、无EMA）（2组）。
  - C组：参考输入模态（仅图像、仅文本）（2组）。
- **进一步分析**：训练/推理效率对比（Table III）、定性检索结果（图4）。
- **充分性判断**：实验设计全面，既在标准基准上对比，又通过消融验证各组件贡献和效率优势。但缺少跨数据集泛化测试（如域迁移）或更大基线模型（如BLIP）的对比。整体公平、客观。

### 6. 论文的主要结论与发现

- DiCE-CIR在CIRCO上大幅超越所有现有方法（ViT-L下mAP@5=20.78 vs 第二名PrediCIR=15.70），达到SOTA；在CIRR上R@5和R@10也最优，但R@1和Rs@K略逊于部分方法（文本基线在Rs@K上表现极强）。
- 直接组合方法彻底解决了投影方法的顺序依赖和训练-推理不一致问题，训练速度比LinCIR快约10.5倍（ViT-L）和约24.3倍（ViT-G）。
- 三个训练目标均不可或缺，其中对比损失对判别性提升最大，残差对齐损失可促进编辑一致性。
- 使用双参考模态（图像+文本嵌入）优于单一模态。

### 7. 优点

- **方法亮点**：
  - 创新性地移除投影分支，直接组合，大幅简化流程、提升效率。
  - 自动化数据构建策略，仅需图像-文本对和LLM，无需人工标注，可扩展性强。
  - 轻量门控MLP参数少且效果好。
- **实验亮点**：
  - 效率对比清晰，量化了训练/推理速度优势。
  - 消融实验全面，验证每个设计的必要性。
  - 在开放检索基准CIRCO上取得显著超越，证明了方法的泛化能力。

### 8. 不足与局限

- **CIRR上部分指标不优**：在Rs@K上弱于文本基线，说明参考图像有时反而干扰，该场景下编辑文本已足够，组合模型可能引入噪声。
- **数据质量依赖LLM**：自动生成样本的多样性、合理性受限于LLM能力，可能存在偏差或不符合真实编辑分布。
- **未评估域迁移**：只在CIRCO/CIRR上测试，未在FashionIQ等垂直领域或跨领域场景验证通用性。
- **骨干冻结**：仅训练组合模块，依赖CLIP的预训练对齐空间，若CLIP本身对细粒度变化敏感度不足，可能限制上限。
- **对比方法覆盖有限**：未与最新扩散类方法（如CIG的变体）或更大规模数据进行充分比较。

（完）
