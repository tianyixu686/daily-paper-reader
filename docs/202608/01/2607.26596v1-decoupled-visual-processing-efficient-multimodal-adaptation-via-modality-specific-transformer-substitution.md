---
title: "Decoupled Visual Processing: Efficient Multimodal Adaptation via Modality-Specific Transformer Substitution"
title_zh: 解耦视觉处理：通过模态特定Transformer替换实现高效多模态适配
authors: "Mingkuan Feng, Zhengqi Wen, Jianhua Tao"
date: 2026-07-29
pdf: "https://arxiv.org/pdf/2607.26596v1"
tags: ["query:post-multi"]
score: 9.0
evidence: 面向视觉指令微调的高效训练框架，将上层解码器替换为视觉专用Transformer块
tldr: 多模态大模型视觉指令微调通常需要全参数更新，计算开销大且深层网络中视觉与文本的表示需求差异显著。为此提出解耦视觉处理（DVP）框架，用轻量可训练的单一Transformer块替换预训练语言模型的上层解码器，专门处理视觉令牌；文本令牌继续通过原冻结层，两流再拼接。训练仅更新该Transformer块，大幅减少可训练参数。在LLaVA-1.5上，DVP在MME、POPE和ChartQA基准上取得有竞争力的性能，表明视觉表示可通过解耦参数高效通路有效学习，为多模态适配提供新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.26596v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1562, \"height\": 326, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.26596v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1577, \"height\": 357, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26596v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 673, \"height\": 311, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26596v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 618, \"height\": 252, \"label\": \"Table\"}]"
motivation: 全参数视觉指令微调昂贵，且深层网络中视觉与文本表示需求差异显著。
method: 将预训练大语言模型上半层替换为轻量单Transformer块，视觉与文本令牌分离后分别处理再拼接，仅训练该块。
result: 在LLaVA-1.5框架上，DVP仅更新少量参数，即在MME、POPE、ChartQA三个基准上取得了与完整微调相当甚至更优的竞争性能。
conclusion: 视觉表示可通过解耦的参数高效通路学习，为多模态适配提供低成本新范式。
---

## 摘要
多模态大语言模型（MLLMs）通过将视觉和文本理解统一在同一个Transformer架构中，展现出了卓越的能力。然而，为了进行视觉指令微调而微调这些模型的所有参数，计算成本高昂且往往没有必要，因为在网络的深层中，视觉和文本标记的表示需求存在显著差异。在本文中，我们提出了解耦视觉处理（DVP），一种高效的训练框架，该框架用轻量级、可独立训练的单个Transformer块替换预训练LLM的上层解码器层，专门用于视觉标记处理。具体来说，在通过解码器层的前半部分进行共享处理后，视觉和文本标记被分离：视觉标记被路由到新初始化的单个Transformer块，而文本标记则继续通过原始冻结的解码器层。然后，这两个流在语言建模头之前进行拼接。在训练期间，仅更新该单个Transformer块，从而大幅减少可训练参数的数量。在LLaVA-1.5框架上的实验表明，DVP在MME、POPE和ChartQA基准上取得了具有竞争力的性能，同时仅训练总参数的一小部分，这表明MLLMs中的视觉表示可以通过解耦的、参数高效的路径进行有效学习。

## Abstract
Multimodal large language models (MLLMs) have demonstrated remarkable capabilities by integrating visual and textual understanding within a unified transformer architecture. However, fine-tuning all parameters of these models for visual instruction tuning is computationally expensive and often unnecessary, as the representation requirements for visual and textual tokens diverge significantly in the deeper layers of the network. In this paper, we propose Decoupled Visual Processing (DVP), an efficient training framework that replaces the upper decoder layers of a pretrained LLM with a lightweight, independently trainable single transformer block dedicated exclusively to visual token processing. Specifically, after shared processing through the first half of the decoder layers, visual and textual tokens are split: visual tokens are routed through a newly initialized single transformer block while textual tokens continue through the original frozen decoder layers. The two streams are then concatenated before the language modeling head. During training, only the single transformer block is updated, dramatically reducing the number of trainable parameters. Experiments on the LLaVA-1.5 framework demonstrate that DVP achieves competitive performance on MME, POPE, and ChartQA benchmarks while training only a fraction of the total parameters, suggesting that visual representations in MLLMs can be effectively learned through a decoupled, parameter-efficient pathway.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：多模态大语言模型（MLLMs）通常将视觉与文本令牌统一送入完整的Transformer解码器栈进行联合处理，然而**全参数微调在视觉指令微调阶段计算开销极大**，且往往并非必要。
- **核心假设**：
  - 解码器的**浅层**主要学习模态无关的通用特征，能够充分完成视觉与文本的跨模态对齐；
  - 解码器的**深层**则高度任务专用化（偏向语言建模），视觉令牌与文本令牌在深层存在**差异显著的表示需求**，视觉令牌不需要与文本令牌共享完整的深度处理路径。
- **整体含义**：该工作提出一种**解耦的视觉处理路径**，用极少的可训练参数实现与全量微调相当的视觉理解性能，为多模态模型的高效适配提供了新的结构性思路。

### 2. 论文提出的方法论

- **核心思想**：提出 **Decoupled Visual Processing（DVP）** 框架，在解码器中部进行模态分离：
  - 前 **K 层**（实验中 K=17）所有令牌共享处理，完成跨模态对齐与上下文融合；
  - 在第 K 层后将序列按模态拆分：
    - **视觉令牌** → 经过一个**新初始化的单层Transformer块**（唯一的可训练模块）；
    - **文本令牌** → 继续经过**原始冻结的解码器层**（第 17~31 层）；
  - 两条路径的输出在语言建模头前**拼接**，继续自回归生成。

- **关键技术细节**：
  - 单Transformer块与原始LLM解码器层具有相同的隐藏维度（d=4096）、注意力头数（32）和前馈网络结构（SwiGLU，中间维度 11008），采用RMSNorm与残差连接；
  - 训练阶段**仅更新该单Transformer块**，所有原始LLM参数（包括共享层）完全冻结；
  - 训练损失仍为标准自回归语言建模损失，仅在文本输出令牌上计算。

- **公式流程（文字说明）**：
  1. 视觉编码器提取特征 → MLP投影到LLM嵌入空间；
  2. 视觉与文本嵌入拼接为完整输入序列；
  3. 前17层共享解码：所有令牌联合通过；
  4. 第17层后拆分：视觉令牌走单Transformer块，文本令牌走第17~31层冻结解码器；
  5. 两路输出拼接，送入LM Head进行下一个文本令牌预测。

### 3. 实验设计

- **数据集**：
  - 训练数据：LLaVA-1.5-665K 视觉指令微调数据集；
  - 视觉编码器：CLIP ViT-L/14（336×336分辨率）；
  - 语言骨干：Vicuna-7B。

- **评估基准**：
  - **MME**（感知 + 认知两个维度，共14个感知子任务和4个认知子任务）；
  - **POPE**（对象幻觉评估，指标为准确率）；
  - **ChartQA**（图表理解与推理）。

- **对比方法**：
  - **LLaVA-1.5-7B**：全量微调基线（7B参数全部更新）；
  - **Normal Training**：使用相同的拆分架构（视觉走单Transformer块、文本走上层解码器），但**所有参数均参与训练**，用于隔离“架构修改”与“参数冻结”各自的贡献；
  - **DVP（Efficient Training）**：本文方法，仅更新单Transformer块。

### 4. 资源与算力

- 论文**未明确报告**GPU型号、数量或具体训练时长等硬件信息；
- 仅提供了**参数效率**对比：DVP可训练参数约 0.2B，约为全量微调的 **1/32（~3.1%）**，相对训练成本约为全量微调的 **0.15×**；
- 计算开销估计停留在参数量层面的理论对比，缺乏实测时间/能耗数据。

### 5. 实验数量与充分性

- **实验数量**：
  - 三个基准（MME、POPE、ChartQA）上的主实验结果；
  - MME感知/认知子分数的详细分解；
  - 与Normal Training的架构对比；
  - 定性讨论（信息流分析、误差分析）；
  - **无系统性消融实验**：论文明确表示 split point 的全面消融、不同Transformer块数量的对比均留作未来工作。

- **充分性与客观性评价**：
  - **正面**：Normal Training对照组的设置合理，能较好地区分“拆分布局”与“参数冻结”两因素的贡献；
  - **不足**：
    - 基准覆盖范围较窄（缺COCO、VQAv2、TextVQA、GQA等通用评测）；
    - 仅7B规模、单一Vicuna骨干、单一训练数据规模，泛化性存疑；
    - 缺少对split point和视觉路径块数的系统消融，使“K=17”和“单块”设计的说服力受限。

### 6. 论文的主要结论与发现

- DVP 在 **MME 感知**上达到 1440，为全量微调 LLaVA-1.5-7B（1496）的 96.3%，而训练参数仅为后者的约 1/32；
- DVP 在 **POPE** 上取得 0.8619，**超过全量微调基线**（0.8577），初步表明解耦视觉路径能**减少对象幻觉**；
- 在 **MME 认知**（327 vs 324）上与全量微调持平；在 **ChartQA** 上优于全量微调（0.2356 vs 0.1776）但显著低于 Normal Training（0.432），说明需深度跨模态逐层交互的任务受限；
- 相比 Normal Training，DVP 感知分数更高（1440 vs 1225），说明**冻结原始LLM参数起到了隐式正则化作用**，避免视觉感知能力被破坏；
- 总体结论：**视觉令牌并不需要完整的深层LLM处理**，一个单层Transformer块即可有效替代15层解码器对视觉令牌的处理，验证了深层网络对视觉令牌的高度冗余性。

### 7. 优点

- **极高的参数效率**：仅约 3.1% 的可训练参数即达到接近全量微调的性能，显著降低训练成本；
- **结构设计简洁新颖**：提出“共享浅层 + 模态分离深层”的 split-process-concatenate 范式，跳出传统逐层 PEFT 的框架，属于**结构性参数高效方法**；
- **对比实验设计较严谨**：设置 Normal Training 对照组，有效隔离架构与冻结策略的各自影响；
- **对幻觉问题有积极意义**：POPE 上的最优表现提示解耦视觉处理可能天然抑制对象幻觉，值得进一步探索；
- **讨论部分富有洞察**：将 DVP 分别关联到MoE式模态路由、知识蒸馏（单块压缩15层知识）、持续学习（模块化即插即用）等，提供了较广的延伸视角。

### 8. 不足与局限

- **基准评测覆盖面窄**：仅 MME、POPE、ChartQA，缺少视觉问答（VQAv2）、图像描述（COCO）、场景文本理解（TextVQA）等主流基准；
- **缺乏系统消融**：split point 选取（K=17）基于“中间层”启发式，未做系统扫描；视觉路径的块数（1 vs 2~4）也未进行实验验证；
- **规模与泛化受限**：仅在7B/Vicuna/LLaVA-1.5-665K 单一配置上验证，未覆盖更大规模（13B、70B）或其他LLM家族（LLaMA-3、Qwen、Mistral）；
- **架构局限**：分离后视觉与文本令牌不再有跨模态注意力，对需要迭代式跨模态精细推理的任务（如ChartQA）构成明显瓶颈；
- **计算资源信息缺失**：未提供 GPU 型号/数量/训练时长，影响对不同方法实际成本的可比性判断；
- **潜在偏差风险**：冻结的预训练组件可能继承原始模型偏见，论文在伦理声明中虽提及但未做任何分析与评测。

（完）
