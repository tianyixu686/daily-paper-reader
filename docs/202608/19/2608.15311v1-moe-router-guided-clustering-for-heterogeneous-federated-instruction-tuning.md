---
title: MoE Router-Guided Clustering for Heterogeneous Federated Instruction Tuning
title_zh: MoE路由引导的异构联邦指令微调聚类
authors: "Ankita Sharma, Bahar Farahani, Sanaz Rahimi Moosavi, Amir Rrahmani, Farshad Firouzi, Krishnendu Chakrabarty"
date: 2026-08-15
pdf: "https://arxiv.org/pdf/2608.15311v1"
tags: ["query:post-multi"]
score: 9.0
evidence: 基于MoE路由指导聚类的联邦指令微调
tldr: 联邦指令微调使大型语言模型适应私有数据，但异构指令分布下盲目聚合会导致负迁移。本文提出ClientMorpher，利用预训练MoE模型的路由签名在聚合前组织客户端协作，包含客户端直接聚类和专家聚类两种互补策略。在Dolly-15K上，该方法相比联邦平均和本地训练均提升个性化性能，且不增加通信成本，为稀疏MoE联邦指令微调提供可扩展方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.15311v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 888, \"height\": 359, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.15311v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1883, \"height\": 302, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.15311v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 874, \"height\": 352, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.15311v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 733, \"height\": 257, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.15311v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 931, \"height\": 196, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.15311v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1875, \"height\": 622, \"label\": \"Table\"}]"
motivation: 异构指令分布下，传统聚合忽视客户端路由行为，引发负迁移，需识别可协作客户端。
method: 利用MoE路由签名设计ClientMorpher，通过客户端专家激活画像或专家跨客户端使用聚类指导分组。
result: 在Dolly-15K上，路由感知聚类优于联邦平均和本地训练，且通信成本不变。
conclusion: 路由感知协作可有效促进个性化联邦指令微调，是稀疏MoE模型的可扩展方案。
---

## 摘要
联邦指令微调使大型语言模型（LLMs）能够适应去中心化、隐私敏感的数据，而无需共享数据。最近的混合专家（MoE）大型语言模型对联邦学习特别有吸引力，因为其稀疏激活在扩展模型容量的同时减少了计算和通信。然而，现有的联邦MoE方法主要关注参数聚合和个性化，忽略了MoE模型的路由行为作为客户端协作信息来源的潜力。在异构指令分布下，不加区分的聚合可能导致负迁移，这凸显了在联邦优化过程中识别哪些客户端应该协作的必要性。我们提出了ClientMorpher，一个路由感知的个性化联邦指令微调框架，它利用预训练MoE模型的路由签名在聚合之前组织客户端协作。我们研究了两种互补的聚类策略：ClientMorpher-C，直接使用专家激活配置文件对客户端进行聚类；以及ClientMorpher-E，首先根据专家的跨客户端使用签名对专家进行聚类，然后得出客户端协作组。我们在Databricks Dolly-15K数据集上评估了ClientMorpher在联邦指令微调中的性能，使用基于病理和Dirichlet的异构客户端分布，涵盖多个指令跟随任务。实验结果表明，与传统的联邦平均和本地训练相比，路由感知协作持续提高了个性化性能，同时保持相同的通信成本。此外，我们的研究表明，以客户端为中心和以专家为中心的聚类为稀疏MoE大型语言模型的个性化联邦指令微调提供了一种有效且可扩展的方法。

## Abstract
Federated instruction fine-tuning enables Large Language Models (LLMs) to adapt to decentralized, privacy-sensitive data without requiring data sharing. Recent Mixture-of-Experts (MoE) LLMs are particularly attractive for federated learning because their sparse activation reduces computation and communication while scaling model capacity. However, existing federated MoE methods primarily focus on parameter aggregation and personalization, overlooking the routing behavior of MoE models as a source of information for client collaboration. Under heterogeneous instruction distributions, indiscriminate aggregation can lead to negative transfer, highlighting the need to identify which clients should collaborate during federated optimization. We propose ClientMorpher, a routing-aware, personalized federated instruction fine-tuning framework that leverages routing signatures from pretrained MoE models to organize client collaboration prior to aggregation. We investigate two complementary clustering strategies: ClientMorpher-C, which directly clusters clients using expert activation profiles, and ClientMorpher-E, which first clusters experts based on their cross-client usage signatures and then derives client collaboration groups. We evaluate ClientMorpher for federated instruction fine-tuning on the Databricks Dolly-15K dataset, using pathological and Dirichlet-based heterogeneous client distributions across multiple instruction-following tasks. Experimental results show that routing-aware collaboration consistently improves personalized performance compared to conventional federated averaging and local training, while maintaining the same communication cost. Furthermore, our study shows that client-centric and expert-centric clustering provides an effective and scalable approach for personalized federated instruction fine-tuning of sparse MoE LLMs.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义

- **研究背景**：联邦指令微调（Federated Instruction Tuning）允许大型语言模型（LLMs）在无需共享原始数据的前提下，利用分散在多个客户端上的私有数据进行适配。混合专家（Mixture-of-Experts, MoE）模型因其稀疏激活特性，可在扩展模型容量的同时显著降低计算与通信开销，成为联邦学习场景中非常有吸引力的基础架构。
- **核心问题**：在联邦微调中，各客户端的数据分布通常高度异构（涉及分类、问答、摘要等不同任务）。传统的联邦平均（FedAvg）将所有客户端的更新无差别地聚合为一个全局模型，忽略了客户端之间的任务差异，容易引发**负迁移（Negative Transfer）**，导致个性化性能下降。现有联邦MoE方法大多只关注参数聚合与个性化策略，**未充分利用MoE模型独特的“路由行为”**作为客户端之间任务相似度或协作关系的结构化信息。
- **整体含义**：论文提出利用MoE路由器在预训练阶段对不同指令分布产生的激活模式（Routing Signatures）来识别客户端之间的分布相似性，并据此在聚合之前预先组织客户端协作（即分簇），以实现更有效的个性化联邦指令微调。

### 2. 论文提出的方法论

- **核心思想**：在冻结的预训练MoE骨干网络上，每个客户端的数据通过其路由器激活的专家组合会形成独特的分布“签名”。激活相似专家的客户端可能具有相似的任务分布，应进行协作；反之则应分开训练。基于此，论文提出 **ClientMorpher** 框架，先聚类、后分簇聚合。
- **关键技术细节**：
  - **相位 0 —— 共享的专家使用画像（Profiling）**：客户端 i 在冻结的MoE模型上运行本地数据的至多 B 个批次，统计其 top-k 路由选择中每个专家被选中的次数，得到归一化的专家使用画像向量 \( u_i \in [0,1]^E \)。所有客户端的画像向量堆叠成客户端-专家使用矩阵 **U**。
  - **变体一：ClientMorpher-C（直接客户端聚类）**：直接对客户端画像向量行 \( u_i \) 执行 K-Means 聚类，将客户端分配到最近的簇中心，形成客户端簇 \( C_k \)。
  - **变体二：ClientMorpher-E（专家重叠聚类）**：先对专家进行聚类。每个专家 e 的签名是其跨客户端的归一化使用列向量 \( s_e = U_{:,e} \)。对专家签名执行 K-Means 聚类，得到专家-簇成员矩阵 **M**。然后，每个客户端被分配到其路由质量（mass）最集中的专家簇，即 \( a_i = \arg\max_k \sum_{e=1}^E u_{i,e} M_{e,k} \)。
  - **相位 2 —— 簇个性化联邦学习**：基于上述任一策略生成的客户端簇，服务器为每个簇维护独立的适应参数状态 \( \theta^{(k)}_a \)。在每个通信轮次中，簇内客户端接收该簇的状态进行本地训练，服务器仅对同一簇内的客户端更新进行加权（样本量）平均聚合。
- **技术优势**：整个过程不交换专家参数，只交换可训练的适配器参数（如LoRA），因此不增加通信成本。路由行为被视为轻量级的、语义丰富的客户端相似性信号。

### 3. 实验设计

- **数据集**：使用 **Databricks Dolly-15k** 指令跟随数据集，涵盖四类任务：文本分类（CLS）、闭域问答（QA）、信息提取（IE）和摘要生成（Summ）。每个样本格式化为Alpaca风格，计算损失时只对响应部分计算。
- **场景/Benchmark**：
  - **病理性划分（Pathological Split）**：4个客户端，每个客户端仅持有单一任务类型的数据（强异构）。
  - **Dirichlet 划分**：8个客户端，使用 Dirichlet 分布抽样任务分布，设置浓度参数 \( \alpha=0.1 \)（高度异构）和 \( \alpha=1.0 \)（相对均衡的非IID）。
- **对比方法**：
  - Local Training（纯本地训练，无通信）。
  - MoE-FedAvg（标准联邦平均，全局聚合）。
  - ClientMorpher-C（本文的客户端直接聚类）。
  - ClientMorpher-E（本文的专家聚类再映射客户端）。
- **评估指标**：ROUGE-L（生成响应与参考答案的匹配度）。

### 4. 资源与算力

- 论文**未明确说明**训练所使用的GPU型号、数量、总训练时长或具体通信轮次（R）与本地步数（L）的设置。仅在文中提到所有联邦方法每轮通信传输的LoRA参数占全量模型的 **0.0878%**。因此，关于硬件的详细配置信息缺失。

### 5. 实验数量与充分性

- **实验组数**：共三类主要实验设置——1组病理性划分（4客户端），2组不同 \(\alpha\) 的Dirichlet划分（8客户端）。共涉及3张结果表（Table II 和 Table III 为核心结果表）。
- **方法维度**：每个实验都对比了4种方法（Local、MoE-FedAvg、C变体、E变体），并报告了按任务类别划分的细粒度ROUGE-L分数。
- **充分性与客观性**：
  - **优点**：实验设置覆盖了极端异构（病理与 \(\alpha=0.1\)）和较温和异构（\(\alpha=1.0\)）场景，评估了任务粒度的表现差异，对比方法公平（所有联邦方法通信预算相同）。
  - **不足**：实验规模较小（客户端数量少、数据集相对较小），论文较简短，**缺乏消融实验**（如不同聚类数K的影响、剖面批次B的影响）；**未与近期的个性化联邦MoE方法（如FedMoE、pFedMoE）直接对比**；也未给出显著性检验或方差分析——结果差异是否统计显著未知。

### 6. 论文的主要结论与发现

- **核心结论**：路由感知的分簇协作在保持相同通信成本的前提下，持续优于本地训练和全局 FedAvg。
- **病理划分中**：ClientMorpher-E 综合表现最佳（平均0.4986），优于 MoE-FedAvg 和 Local Training，在分类、QA、摘要上均取第一；ClientMorpher-C 在信息抽取上最强。
- **Dirichlet 异构条件下**：两种变体均表现良好，且**具有互补性**——在 \(\alpha=0.1\) 高度异构下 ClientMorpher-C 在摘要上领先，ClientMorpher-E 在分类上更好；当 \(\alpha=1.0\) 分布更均衡时，Expert 聚类（E变体）产生的协作组更稳定，平均分更高。
- **理论启示**：客户端直接聚类和专家聚类捕获了任务相似性的不同维度，前者适合路由模式高度分离的场景，后者更适合分布相对平滑的场景。

### 7. 优点

- **新颖视角**：首次系统性地将 MoE 路由器的**行为作为联邦学习中客户端相似性的先验信号**，在聚合之前利用该信号进行分组，不同于以往仅将路由用于计算稀疏性或个性化适配的做法。
- **设计统一且高效**：两种变体共享同一框架和训练流程，仅在聚类层不同，对比清晰。通信轮次中只传输 LoRA 参数，**不引入额外通信开销**。
- **方案可解释性**：通过专家激活频率和专家-客户端共现矩阵，直观地构建了客户端与模型内部结构的语义映射关系。
- **互补策略探索**：清晰展示了客户端视角（expert profile）和模型视角（expert usage）聚类各自的适用场景。

### 8. 不足与局限

- **实验规模有限**：客户端数量（4/8）和总数据量较小，无法完全反映大规模联邦系统（数十至数百客户端）下的复杂通信和异构情况。
- **缺乏消融与超参分析**：未分析聚类数 K、剖面批次 B、本地训练轮数等关键超参的影响。
- **对比基线不够全面**：未与现有的联邦MoE基线（如 FedMoE、PFL-MoE、FedMoE-DA）和经典个性化FL方法（如 IFCA、CFL）进行对比，导致“性能提升”结论的基线支撑较弱。
- **指标与验证边界**：仅依赖 ROUGE-L 单一指标，缺乏 BLEU、语义相似度、人工评估等多维度评价；未讨论路由签名分布的可视化或噪声鲁棒性。
- **隐私与安全**：虽然利用路由行为，但未分析该路由特征是否会泄露客户端数据的隐私信息（如梯度或预测模式的逆向攻击风险）。
- **论文内容可能不完整**：篇幅较短（6页），缺少实现细节、训练参数表、损失曲线等验证性细节，部分图表和公式排版在提取文本中略显粗粒度。

（完）
