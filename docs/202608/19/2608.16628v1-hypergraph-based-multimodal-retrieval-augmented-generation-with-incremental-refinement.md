---
title: Hypergraph-based Multimodal Retrieval-Augmented Generation with Incremental Refinement
title_zh: 基于超图的多模态检索增强生成与增量细化
authors: "Shenao Chen, Yidan Xu, Xiangmin Han, Rundong Xue, Duanpo Wu, Yuhan Gao, Chenggang Yan, Yue Gao"
date: 2026-08-17
pdf: "https://arxiv.org/pdf/2608.16628v1"
tags: ["query:mr"]
score: 9.0
evidence: 明确提出基于超图的多模态检索增强生成
tldr: 现有M-RAG因采用简单图而难以捕获文本-图像-表格间的高阶N元关联，且细化常需全页重建，计算冗余并引入噪声。为此提出Hyper-M2RAG，通过超边封装多模态语义、构建多模态超图，并设计锚点驱动增量细化，仅对跨页锚节点做局部重构，高效弥合跨页知识。基准实验表明，其在检索与生成指标上远超SOTA。该框架为多模态文档理解提供了更精确、高效的新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.16628v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1790, \"height\": 911, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.16628v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 814, \"height\": 257, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.16628v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1801, \"height\": 733, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.16628v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 813, \"height\": 259, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.16628v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 857, \"height\": 258, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.16628v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1853, \"height\": 276, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.16628v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 905, \"height\": 137, \"label\": \"Table\"}]"
motivation: 传统简单图无法建模多模态文档中异构实体间的高阶关联，且全页细化冗余，制约M-RAG性能。
method: 提出超图表示学习+锚点增量细化，以超边统一封装跨模态关联，仅重构跨页锚节点邻域。
result: 在基准集上，检索精度与生成连贯性均显著超越现有最先进方法。
conclusion: 超图建模与局部增量机制结合，可高效提升多模态文档理解与生成质量。
---

## 摘要
现代多模态检索增强生成（M-RAG）系统从根本上受到传统简单图二元连接范式的限制，这种范式无法捕捉异构实体之间错综复杂的高阶关联，例如视觉图表、其分散的文本描述以及底层数值数据之间的N元关系。此外，现有的细化策略通常依赖于详尽的全页重构来对齐跨模态信息，导致在长文档处理中产生过高的计算冗余并引入上下文噪声。在本文中，我们提出Hyper-M2RAG，一种通过高阶超图表示学习重新定义多模态文档检索的新颖框架。我们首先将文档结构形式化为多模态超图，利用超边作为统一语义容器来封装文本、图像和表格之间的多路关联，从而超越点对点建模。为了缓解物理分页造成的语义碎片化，我们引入了一种锚点驱动的增量细化机制。该方法不是进行全局扫描，而是识别跨边界锚节点，并使用一跳邻域上下文重建其局部超拓扑。这种定向细化以最小的计算足迹有效弥合了跨页知识鸿沟。在多模态基准数据集上的广泛评估表明，Hyper-M2RAG在检索精度和生成连贯性方面均显著优于最先进的方法。我们的代码可在https://github.com/ShenAoChen2001/MMHRAG获取。

## Abstract
Modern Multimodal Retrieval-Augmented Generation (M-RAG) systems are fundamentally limited by the binary connectivity paradigm of traditional simple graphs, which fails to capture the intricate, high-order correlations among heterogeneous entities, such as the N-ary relationships between a visual chart, its scattered textual descriptions, and underlying numerical data. Furthermore, existing refinement strategies often rely on exhaustive, full-page reconstruction to align cross-modal information, leading to prohibitive computational redundancy and the introduction of contextual noise in long-form document processing. In this paper, we propose Hyper-M2RAG, a novel framework that redefines multimodal document retrieval through High-order Hypergraph Representation Learning. We first formalize the document structure as a Multimodal Hypergraph, utilizing hyperedges as unified semantic containers to encapsulate multi-way associations across text, images, and tables, thereby transcending point-to-point modeling. To mitigate semantic fragmentation caused by physical pagination, we introduce an Anchor-driven Incremental Refinement mechanism. Rather than performing a global sweep, our approach identifies boundary-crossing anchor nodes and reconstructs their local hyper-topology using one-hop neighborhood contexts. This targeted refinement effectively bridges cross-page knowledge gaps with minimal computational footprints. Extensive evaluations on multimodal benchmarking datasets demonstrate that Hyper-M2RAG significantly outperforms state-of-the-art methods in both retrieval precision and generation coherence. Our code is available at https://github.com/ShenAoChen2001/MMHRAG.

---

## 论文详细总结（自动生成）

# 基于超图的多模态检索增强生成与增量细化：论文总结

## 1. 核心问题与整体含义

- **研究动机**：多模态文档（如学术论文、技术报告、商业文档）中的关键证据常分布在文本、图表、表格等异构元素中，且可能跨越多页。传统检索增强生成（RAG）系统多基于文本中心假设，用纯文本代理（如标题或OCR文本）检索，丢失了视觉语义和布局结构。
- **根本局限**：传统图结构（如知识图谱）采用二元关系（头、关系、尾）建模，无法表达多模态文档中“N元关系”的联合交互（例如同一证据由图表区域、标题、段落、表格单元格共同构成）。将高阶关系拆解为二元边会割裂语义单元，降低下游推理可靠性。
- **研究意义**：本文主张将RAG的结构基础从“图”升级为“超图”，以超边统一封装文本、图像、表格等多模态元素之间的高阶关联，同时解决长文档中跨页语义碎片化与低效细化问题。

## 2. 方法论

### 核心思想
提出 **Hyper-M2RAG** 框架，通过“多模态超图表示学习”和“锚点驱动增量细化”实现高效、结构感知的多模态检索与生成。

### 关键流程（三阶段）

1. **多模态数据摄入（PMU）**：
   - 以“页面级多模态单元（PMU）”作为原子单位，使用 MinerU 等解析工具从PDF中提取文本、全局页面图像、局部图像块（图表区域）。
   - 视觉输入分为宏观布局上下文（整页图像）和细粒度证据（局部图像块），并支持纯文本页面（视觉字段可空）。

2. **两阶段超图索引构建**：
   - **初始页面级索引**：对每个PMU调用LLM提取实体集 \(V_i\)、二元关系边 \(E_{i,low}\) 和高阶关联边 \(E_{i,high}\)，并记录来源页索引。
   - **合并**：通过规范化合并顶点、按顶点集合折叠超边，得到全局初始超图 \(G_{init}\)。
   - **锚点驱动结构细化**：
     - 识别跨页锚点 \(V_{anc}\)：在多个页面出现的实体（来源频率 ≥ τ）。
     - 对每个锚点提取其“星展开子图”（所有包含该锚点的超边及其邻接顶点）。
     - 用LLM只在该局部子图上合成跨页关联边 \(E_{refine}\)。
     - 将细化后的超边合并回全局超图，形成最终 \(G_{final}\)。

3. **认知启发式混合检索**：
   - **查询分解**：将用户查询分解为细粒度实体关键词 \(K_{low}\) 和主题级关键词 \(K_{high}\)。
   - **双通道检索**：
     - **顶点锚定实体检索**：基于实体关键词相似度找到相关顶点，再扩展其关联超边得到 \(C_{ent}\)。
     - **超边导向关系检索**：基于主题关键词检索高阶超边，回溯其顶点和来源文本得到 \(C_{rel}\)。
   - **证据融合与生成**：将两路证据整合为结构化证据包，送入LLM生成最终答案。

### 核心公式（文字说明）
- 文档表示为超图 \(G=(V,E)\)，\(E=\{E_{low}, E_{high}, E_{refine}\}\)。
- 检索过程：\(S = HyperRetriever(G,Q)\)，同时覆盖实体证据和关系上下文。
- 锚点定义：\(V_{anc}=\{v \mid |src(v)| \ge \tau\}\)。
- 星展开子图：\(E_{sub}(v)=\{e \in E \mid v \in e\}\)，\(V_{sub}(v)=\{u \mid \exists e \in E_{sub}(v), u \in e\}\)。

## 3. 实验设计

### 数据集与场景

**全局QA（整本书/整本报告级）**
- 纯文本语料：UltraDomain的Mixed-Domain（约0.62M tokens）、MedRAG的NeurologyCorp（约1.97M tokens）。
- 多模态语料：自建的World History教科书（788页）和Sustainable Development Report（491页幻灯片），问题需跨视觉元素回答。
- 每个数据集通过自动化协议生成125个全局问题（文本用多用户、多任务脚手架；多模态强制依赖图表/布局）。

**局部QA（页面/幻灯片级）**
- 使用RealMMBench的TechReport（1,674页）和TechSlides（1,963页）子集，评估细粒度检索精度。

### 对比方法
- GraphRAG（知识图谱RAG）
- HyperRAG（纯文本超图RAG）
- MegaRAG（多模态图RAG）
- 消融基线：HyperRAG + MM Extraction、HyperRAG + Anchor Refinement

### 评价指标
- 全局QA：LLM法官对比胜率（采用“双响应交换与平均”策略消除位置偏差），维度包括Comprehensiveness、Diversity、Empowerment、Overall。
- 局部QA：语义一致性Accuracy。

## 4. 资源与算力

论文**未明确说明**使用的GPU型号、数量或训练/推理时长。仅提供了以下配置信息：
- 生成模型：Qwen3-VL-8B
- 问题合成与评估模型：DeepSeek-v3
- 嵌入模型：gme-Qwen2-VL-2B-Instruct
- 文档解析：MinerU2.5
- 效率对比中给出“World History语料每100页的构建与细化时间（12分钟）以及token消耗（765.9k）”，但这属于方法效率指标，并非算力清单。

## 5. 实验数量与充分性

### 实验组数
1. **全局QA对比**：4个域（2文本+2多模态）× 3个基线，报告了4个维度胜率。
2. **局部QA对比**：2个数据集× 3个基线，报告Accuracy。
3. **消融研究**：从HyperRAG基线逐步加入MM Extraction和Anchor Refinement，在两个局部QA数据集上评估。
4. **结构复杂性分析**：比较不同方法的低阶/高阶超边数量。
5. **跨模型法官审计**：用Kimi-K2.6、GLM-5.1、DeepSeek-V4 Pro三个不同LLM重复评估，确认结论不依赖单一法官。
6. **效率分析**：比较构建时间、token消耗。

### 充分性评价
- **优点**：覆盖面较广，兼顾文本与多模态、全局与局部、检索质量与生成质量，并有消融和审计实验，设计相对严谨。
- **客观性风险**：全局QA无真实标准答案，依赖LLM评估（虽然有交换策略缓解偏差）；多模态数据集为自建，可能缺乏外部标准；对比基线中未包含更新的多模态RAG方法（如MHier-RAG等）；胜负率表格中Tie占比高，可能掩盖部分差异。

## 6. 主要结论与发现

- **超图优于二元图**：Hyper-M2RAG在全局QA各维度胜率均优于GraphRAG、HyperRAG和MegaRAG，尤其在多模态场景中优势显著（如Sustainable Report域对MegaRAG Overall胜率88.8%）。
- **多模态超图带来的结构性收益**：相比纯文本超图HyperRAG，Hyper-M2RAG在Overall胜率上提升22.4%~44.8%，表明布局感知的超边构建和锚点细化能增强跨模态语义一致性。
- **局部检索精度提升**：在TechReport/TechSlides上准确率分别达70.0%/78.0%，较MegaRAG提升2~9个百分点，说明高阶超图能更好聚类空间中分离但语义相关的元素。
- **效率优势**：锚点驱动细化将跨页关联发现限制在局部邻域，token消耗比MegaRAG降低约72%，比“HyperRAG+全页细化”降低约77%，且构建时间更短。
- **鲁棒性**：跨模型法官审计结果一致，说明性能提升并非单一评估模型的偏差。

## 7. 优点

- **问题定义清晰**：准确指出传统二元图对多模态高阶关系的缺陷，并给出结构化解决方案。
- **方法新颖且完整**：从“图”到“超图”的转变是自然但重要的扩展，结合了多模态解析、拓扑超图构建和局部增量细化，形成完整pipeline。
- **效率设计合理**：锚点驱动的星扩展细化避免了全页重扫描，兼具理论意义和实用价值。
- **实验设计较规范**：考虑了位置偏差（交换评估）、法官多样性（跨模型审计）、结构统计解释（高阶超边数量），并给出了消融和效率分析。
- **开放代码**：提供GitHub仓库，便于复现和进一步研究。

## 8. 不足与局限

- **算力信息缺失**：未报告GPU资源、训练时长等，难以评估实际部署成本。
- **全局QA评估依赖LLM法官**：即使有交换策略，仍可能受法官模型自身偏好影响；且Tie率有时较高，结果区分度有限。
- **多模态数据集自建**：缺乏第三方标准基准，问题生成协议可能引入隐性偏见；基准规模（125题/数据集）相对较小。
- **对比基线范围有限**：未与近期更新的多模态RAG系统（如MHier-RAG、RAG-Anything等）直接对比，也未与端到端长上下文LVLM比较。
- **图像/视觉信息使用深度**：虽然设计了PMU结构，但最终查询和关系提取依然依赖文本型LLM（MinerU解析结果），未充分展示联合视觉-语言编码的收益。
- **锚定阈值τ的选择**：文中未报告τ的敏感性分析，其影响跨页关联发现的质量和效率。

（完）
